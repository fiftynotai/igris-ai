# Changelog

All notable changes to the `igris-ai` CLI will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

---


## [Unreleased]

### Added

- **The OS measures itself: `igris kpi` and `igris ceremony` (FR-268)** — seven OS KPIs computed on read from the brain's own records, and one NEW record: the brain-timed ceremony stamp.
  - **`igris kpi [--project <slug>] [--weeks N] [--json] [--sql] [--alarm]`** — capacity (brain-bracket agent minutes per project per week), throughput (Done per week and per active day), effort mix of Done, minutes per hunt by phase (nearest-rank median / p75), rounds per hunt, model per role, and ceremony cost — all from `hunt_runs`, `brief_status` and the new `ceremony_runs`. Weeks are **Monday–Sunday UTC**; the current partial week is included and marked. Read-only door only (`query_only = ON`); `--sql` prints the seven derivations plus the KPI 7 coverage sub-query (8 statements) verbatim for `sqlite3`; `--alarm` prints the one week-over-week line `/scan` renders (`!` when |Δ| > 30 %). Nothing is stored — the rows are the record, the rollups are SQL.
  - **`igris ceremony start|stop --name boot|rest|register|hunt-init`** — writes `ceremony_events` (instances migration **v4**, with the `ceremony_runs` view) through the CLI's local write door; `created_at` is the DB clock and `duration_ms` is SQL-computed on stop from the paired open start — the verb never passes a timestamp. `/boot`, `/rest`, `/register` and `/hunt` INIT call it as their first and last executable step; `scripts/validate_ceremony_sites.sh` hard-fails in `pre-commit` when a skill loses a site, and `igris kpi` reports unpaired starts per week.
  - **`ceremony_events` joins `SYNC_TABLES`** (append; key `machine_hostname, project, ceremony, event_type, created_at`) and the egress manifest. **Deploy the remote FIRST:** a remote without instances v4 SKIPS the table on push (not a per-row 207), and the local `sync_state` watermark still advances — see `docs/reference/os-kpis.md` § residuals for the recovery.
  - Durable like `agent_events`: no purge, no TTL, no cap (the no-purge scan now covers both tables). `igris export` lists it under the deliberate exclusions.
  - **Filed from this hunt:** **BR-097** — the push-watermark skip above (a push should refuse to stamp a table the remote did not acknowledge). **TD-431** — the first FULL post-ship UTC week is 2026-08-31 → 09-06; `igris kpi --weeks 1` and the `--alarm` line are read on or after 2026-09-07 UTC and appended to `docs/reference/os-kpis.md` §9.

### Fixed

- **The VPS brain was four schema versions behind, and is now current (TD-378)** — BR-083's qualifiers join `SYNC_TABLES`' `syncKey`, so the remote had to reach `edges@4` before any push. Measuring first found the gap was larger than filed: **`schema_version` 21 (not 24), `edges@3`, no qualifier columns** — TD-338's recorded v22/v23 gap PLUS v24 and v25, which nobody had listed.
  - **Deployed via `igris sync code`, which is code-only** — rsync, `npm ci`, build, native-module smoke check, PM2 restart, health. Migrations run at brain boot, so the restart carried them. The smoke check passed BEFORE the restart, which is the invariant that keeps the old brain serving if a freshly-installed binding cannot load — the same property BR-089's build-then-swap preserved two commits earlier.
  - **Result, verified read-only:** `schema_version` 21 → **25**, `edges@3` → **4**, columns now end `from_project to_project`, **770 edges and 670 learnings unchanged**, `integrity_check` ok, `/health` `{"status":"ok"}`, PM2 online. Six backups on the remote — five written by the migrations' own pre-hooks, one taken by hand and verified first (`integrity ok`, 770/770, 670/670). **TD-338's gap is closed by the same deploy.**
  - **THE PUSH WAS STOPPED, AND THIS BRIEF'S OWN AC WAS THE PROBLEM.** It read *"a push completes without error."* It would. `mergeRows` keys on the FULL syncKey, so a locally-qualified row cannot match the same logical edge stored unqualified — measured on the live VPS: the qualified key matches **0** rows, the unqualified one matches **1**. It takes the INSERT branch, `strategy: 'append'` never removes the older row, and up to **458 edges duplicate**. `GL-006` on the remote would then show the fused row AND the qualified one — strictly worse than the bug BR-083 fixed. `INSERT OR IGNORE` on a key that does not collide is not an error; it is a silent duplication, and the criterion written to prevent it could not see it. Filed as **BR-090**; nothing was pushed.
  - **The obvious workaround is also wrong, and that was measured rather than assumed.** Running the remote's own backfill attributes **295/770 (38.3%)** against the local's **458/785 (58.3%)** — its corpus is thinner (670 learnings vs 1067), so `C6 ambiguous_unresolved` is 72 there against 1 locally. Worse attributions, and still 475 rows left to duplicate.
  - **Eight orphan `schedule_runs` rows attributed but not swept** — real historical run records whose parent schedules were deleted, `entity_edges` clean at zero. Whether a delete path bypasses the constraint (a live bug) or they predate `foreign_keys = ON` (an artifact) decides delete-vs-fix, and the newer rowids make the comfortable explanation the less likely one → **TD-379**.


- **`entity_edges` is project-qualified — 74.6% of brief edges were cross-project ambiguous (BR-083)** — a brief ID is unique only WITHIN a project, and the edge table stored a bare `(type, id)` pair with no project column anywhere. `igris_edge_create` actively REFUSED the qualification (`unknown argument 'from_project'`), so an unqualified edge was not an oversight at the call site — it was the only edge the API could express. Last open member of GL-006.
  - **Re-measured before scoping, because the brief's figures were eleven days old.** 543 brief edges → **690**; 413 ambiguous (76%) → **515 (74.6%)**; 365 colliding ids → **395**, across 35 projects. The ratio held while the corpus grew ~27% — this is a defect that grows with the brain.
  - **Nullable in DDL, conditionally required in the handler**, and the condition is EMPIRICAL rather than a type list: a qualifier is required iff the id exists in 2+ projects, via BR-078's existing resolver. `|P|=1` resolves for free, so the overwhelming majority of existing call sites keep working unchanged; only a genuinely ambiguous endpoint hard-fails, and that failure IS the bug, surfaced at the moment of minting. `traversal.ts` now delegates to the same ladder, so BR-078's "one dialect" holds by construction rather than by copied strings.
  - **Enforced in `handleEdgeCreate`, not the gateway — an operator ruling, with the argument recorded at the code.** The rule needs a `brief_status` lookup, so it can be neither JSON-Schema `required` nor a DDL `CHECK`. And a gateway-only rule would have missed **nine in-process callers** that never touch `dispatch()` — BR-080's silent-drop class one layer down. `handleEdgeCreate` IS the choke point for this table.
  - **The backfill attributed 458 of 785 (58.3%) and deliberately left 327 (41.7%) NULL.** Provable is not a new rule — it is `resolveEdgeProjects(...).resolution === 'unique'`, the brain's own verdict. **`edge_resolution` is byte-identical before and after**, which is the strongest available evidence that nothing was guessed. Every row gets a JSONL line, attributed or refused; a re-run reports 458 `already_qualified` and 0 new. A wrong attribution is worse than a null, and 41.7% is the honest headline rather than a shortfall.
  - **AC-2 was restated as a PROPERTY because the original had already gone stale.** It said "returns exactly the seven `igris-ai` briefs"; GL-006 had 32 serving edges by the time this ran. Now: **44 rows → 32**, every one `igris-ai`, and `BR-082/083/084/085` — each of which existed in FOUR projects — appear once, resolving to the right brief. A count goes stale; the property will not.
  - **The vacuous-gate trap, quantified rather than described.** The brief named it: a fixture with unique ids passes before AND after. `BR-999` in two projects on two different goals gives `COLLIDING: pre-fix 2, post-fix 1` versus `UNIQUE: pre-fix 1, post-fix 1`. Genuine red against the checked-out pre-fix reader. **A self-negative-control ships** — it runs the old join and asserts 2, so weakening the fixture back to unique ids fails the suite. Plus a **storage-level red** no read-path test could reach: two edges differing only by project store as two rows, and the v1 table collapses them to one.
  - **The migration ran on the live 59 MB brain only after the operator reviewed the dry-run counts.** Two independent verified backups; the backfill script REFUSES `--apply` without `--snapshot` naming one — *"A backup nobody named is not a backup — it is a hope."* 785 rows before and after, `integrity_check` ok, zero FK violations.
  - **`unresolved_hops` and `edge_resolution` were NARROWED, not retired**, and FR-240's composite node key was explicitly kept: it encodes NODE identity, and retiring it would re-fuse 35 projects' `BR-001`.
  - **A fixture had silently stopped mirroring the schema.** `dashboard-layers-fixture.ts` still built `entity_edges` at v1 while the reader began selecting `e.from_project` — so `/api/goal` returned `goal: undefined`, a fixture-only fault that reads exactly like a broken reader. Caught by two tests whose assertions were about serving briefs, not schema drift.
  - **Deploy-ordering hazard, recorded:** the qualifiers join `SYNC_TABLES`' `syncKey`, so **the VPS must migrate to `edges@4` before the next push** or every INSERT fails on unknown columns. Related: nullable syncKey columns broke `= ?` in both `mergeRows` doors — `col = NULL` is never true, so every pull would have appended a duplicate of the unattributed 42%. Moved to `IS`.


- **The dashboard's `attach_goal` now forwards the project half of the ref (BR-083)** — `TRIAGE_ACTIONS.attach_goal.refKeys` forwarded `brief_id` ALONE, and that omission was recorded as the BR-078 asymmetry *written down where the ambiguity is minted*. `entity_edges` gained `from_project`, so the ref's `project` reaches the tool and the drop is closed. The browser already sent `{project, brief_id}` refs — this is a server-side map edit with **zero browser bytes**, and the map is still eight rows, eight tools, eight dispatches.
  - **The forward is load-bearing now, not cosmetic.** `handleEdgeCreate` REFUSES an endpoint whose id lives in more than one project, so dropping the project half would turn a silently-wrong edge into a hard failure at the next attach.
  - **`EXPORT_TABLES` gained the same two columns and the same syncKey**, verified as a verbatim `SYNC_TABLES` mirror rather than assumed. The local-row lookup moved from `=` to `IS` — these are the first NULLABLE key columns and `col = NULL` is never true, so every import would have classified an unattributed edge as NEW and appended a duplicate — and the key is intersected with the columns that actually exist, so an older pack still imports as NULL instead of throwing.

- **`npm install -g igris-ai` failed on Node 26, and our own manifest was the bug (BR-089)** — the first install report this project has had from outside this machine. `better-sqlite3` had no prebuilt binary for `target=26.3.0`, fell back to `node-gyp`, and died. We pinned `^11`, which declares **no `engines` at all**; we declared `>=20.0.0` with no upper bound, so Node 26 satisfied it, npm installed happily, and it broke in a native build. **Our engines field promised a range we did not deliver, and pre-declared every future Node major as supported before it existed.**
  - **`^12`, not `^13`, and not unpinned.** v12 declares `20.x || 22.x || … || 26.x` — exactly the range we already advertise — so the bump makes our claim TRUE rather than narrowing it. v13 requires `>=22` and would silently break the Node 20 floor. And unpinning was considered and rejected: `^11.0.0` was ALREADY a range, npm was already picking the newest 11.x; a floating range resolves to 13.0.3 today, and with npm's default `engine-strict=false` (verified) a Node 20 user gets a WARNING then a broken install. That trades one broken Node for another and makes the native driver non-reproducible between two installs a week apart.
  - **THE BUMP BROKE THE BRAIN, exactly where the brief said the risk was.** `SqliteError: error in trigger learnings_vec_ad: unsafe use of virtual table "learnings_vec"` at the v19 `ALTER TABLE registry RENAME TO catalog`. `db.ts` sets `PRAGMA trusted_schema = OFF` as deliberate hardening; with it OFF, an `ALTER TABLE … RENAME` — which makes SQLite re-parse EVERY trigger in the schema — is refused when any trigger touches a `vec0` virtual table. **v12 bundles a newer SQLite that enforces what v11 tolerated.**
  - **Measured three ways, not inferred.** Full brain suite: **161/161 on v11, 2 red on v12** with only the driver changed. A ten-line repro: `trusted_schema=ON` + ALTER passes, `OFF` fails with the identical message, `OFF`→`ON`→`OFF` passes. And `sqlite-vec` itself is fine under v12 — `vec_version = v0.1.9`, with a `vec0` insert and `MATCH` returning `{"rowid":1,"distance":0}`. (A first probe failed on rowid binding; that was the probe's bug — `vec0` rowids need `BigInt` — not v12's.)
  - **AND FIXING THE CALL SITE WAS NOT THE FIX.** Wrapping `getDb()` made `getDb()` pass and left **64 cli assertions** failing, all reporting `brain write engine boot failed` and none naming a pragma. A pragma is a property of a CONNECTION, and `migrateSchema` has **two callers on two different connections** — `getDb()`'s singleton and `bootEngine()`'s adapter handle. The toggle now lives INSIDE `migrateSchema`, scoped in time with a `finally`, which is the only place a third caller cannot forget it. TD-098 met the same guard from the other side two months ago and dropped the offending trigger; that fix stands — this is the same guard on a different operation.
  - **`engines` is now `>=20.0.0 <27.0.0`, and that is only half a fix.** npm defaults to `engine-strict=false`, so it **warns and proceeds** — it does not refuse. A hard stop needs a `preinstall` guard, and whether npm runs one BEFORE building a dependency's native module is version-dependent and could not be verified here. Shipping a guard never shown to fire is the failure this repo has spent a week removing, so the gap is recorded rather than papered over → **TD-377**.
  - **Collateral, and the validator earned its keep:** TD-334's line-number check caught `MAINTAINING.md` citing `db.ts:289` at a blank line, because this fix added ~33 lines above it. Re-pointed to `:343`/`:346`.
  - **The finding worth more than the fix: nothing here tests `npm install` at all.** This repo has a browser gate with red-first mutation testing, a packed-size ledger measured last on every brief, and a contract map with line-number validation — and none of it covers the one command every new user runs first, on a machine that is not this one. The declared support range spans four Node majors; CI exercises one. A stranger found it in a single command.

### Added

- **A `diagnostics` route, and the cognition panel behind it (FR-266)** — Igris fails quietly: on 2026-08-24/25 a single session found seven live problems and six of them BY ACCIDENT. `/boot` renders some of it once, into a terminal, where it scrolls away. This is the first surface you can glance at. The spine plus ONE panel; each further panel is its own brief and none blocks the others.
  - **ONE new endpoint, `GET /api/cognition`, and it FORWARDS the digest verbatim.** No re-map, no rename, no field selection — `cognition` is exactly what `verbs/cognition.ts#buildCognitionHealthDigest` returned, so a field added brain-side reaches the browser with **zero edit to the dashboard tier**. That is the derived form of the rule TD-327 was filed for: a hand-list over an OPEN registry cannot report on the members nobody remembered to list.
  - **IN-PROCESS, NOT A SUBPROCESS, and the alternative was costed rather than dismissed.** `spawn("igris", …)` loses on five independent grounds, and the load-bearing one is that a child process opens a brain handle `dashboard-readonly.test.ts` **cannot inspect** — so "read-only is mechanically true" would have degraded to "true in a process we did not measure". It would also read the operator's REAL brain unless `IGRIS_BRAIN_DIR` were threaded in by hand at every call site, and needs `igris` on the `PATH` of the serving process, which neither the packed-tarball smoke test nor the browser gate can assume. In-process is safe HERE specifically because the verb reaches the brain only through `withReadonlyBrain` → `openBrainReadonly` — the same door with `query_only = ON` — so the endpoint inherits the structural guarantee instead of promising one, and joins the read-only crawl.
  - **THE PANEL WAS MEASURED AGAINST A REAL FAILURE, not only against a healthy render.** The 2026-08-24 state is reproduced in the shape the classifier actually produces: one `failing`, one `wedged`, and the two instances the wedged one drives reported `blocked_upstream` rather than silent — plus one deliberate `disabled`, so both sides of "a failure is not an operator choice" live in ONE fixture. **Five of the six statuses in one world.** A real browser reads it back: `#/diagnostics` renders `tones=["alarm","attention","off","ok"]` over `statuses=["blocked_upstream","disabled","failing","ok","wedged"]`, with the failing gate key verbatim. An **un-wedge control** flips one cell in `schedule_runs` and requires three verdicts to move while the rows that depend on neither stay put — without it the statuses could be an artifact of a seeder that answered the same thing for any input.
  - **`blocked_upstream` IS DELIBERATELY NOT RED.** A co-driven instance runs only inside its driver's run and has no switch or schedule of its own; `classify()` exists precisely so the operator is sent to the DRIVER. Painting it red re-creates that mistake in pixels. The tone lives in a PURE module and is pinned by mutation — flipping it to `alarm` reddens a named test.
  - **`toneFor` is TOTAL over `string`, not over the union.** The registry is OPEN and the brain/CLI pair is not upgraded atomically on a running machine, so a seventh status can reach an older client. A `switch` with no default renders a BLANK chip, which is worse than a wrong one — a blank says nothing is wrong. Unknown → attention, with the raw value shown and the panel SAYING it did not recognise it.
  - **It degrades to a stated unknown in FOUR reachable states, and the fourth needed a mechanism rather than a branch.** Brain absent, roster table absent (a different remedy — collapsing the two hides which applies), malformed JSON, and *never settles*: an `AbortController` fired by a deadline turns a hung read into `read timed out after 15s`. TD-405's eternal loader is the recorded failure this must not repeat, and `payload === null && !loading` rendering `variant="loading"` is asserted **absent**.
  - **The roster is derived AS A PROPERTY OF THE FILES, not only of the data.** A hardcoded roster that happened to match the fixture would pass every data test, so a source scan asserts no shipped client file uses an instance id as a standalone literal, and the diagnostics files name none at all. The scan was NARROWED to that shape after a first draft flagged three legitimate PROSE sentences — and the narrowing is itself asserted, because a scan that stopped discriminating would be vacuous.
  - **A LATENT HARNESS TRAP, FOUND AND CLOSED.** `dashboard-layers-source.test.ts` locates its CSS scan with `css.indexOf("<marker>")`, which cannot tell a QUOTATION from the marker. A comment placed above the real marker that spelled it out verbatim moved the scan's start ~900 lines earlier and swallowed the whole ported brand region — two assertions red for a reason unrelated to the rule. The comment now names the marker without reproducing it, and says why.
  - **THE INITIAL SET STOPPED BEING ONE FILE, which is TD-347's own caveat arriving.** A fifth lazy route made `lib/api.ts` shared across enough boundaries that Rollup hoisted it out of the entry and Vite emitted a `modulepreload` link: the entry FILE shrank **10_501 B** while the initial LOAD grew **381 B**. A gate measuring the entry file would have recorded a 3.7% improvement for a brief that cost bytes. `ui/Badge` left `useQFilter` for its own chunk on becoming a third async importer. **Neither ceiling was re-based** — `MEASURED_*` is the TD-347 baseline and `HEADROOM` is what briefs spend against it, so moving it up is a ceiling raise wearing the words "re-measured". TOTAL 580_979 B against 586_923 B: **5_944 B of slack left** — 45 B more than FR-247's 5_899 B, the largest single-brief chunk spend the ledger records, so an FR-247-shaped brief would *just* fit. That ledger starts at FR-246 though, and FR-240 spent ~+47_700 B in one brief, so the largest on record is the largest in a sample that excludes the only brief big enough to bust the ceiling.
  - **Read-only, by construction and by coverage.** `dashboard-readonly.test.ts` passes UNCHANGED in its guarantee and STRICTER in its reach: the new path joins the crawl (with the cognition world seeded, so the four readers behind the digest actually run rather than short-circuiting on an absent table), and `POST /api/cognition` joins the method loop. The surface still has **exactly one** write endpoint.
  - **Deliberately NOT done, with the reason measured.** `evaluateGates` returns the same `disabled_by` for an ABSENT key and an EXPLICIT `false`, so **no render can distinguish "never enabled" from "deliberately switched off"**. Deriving it in the dashboard would put a second resolver of gate semantics in a read tier. The panel shows `disabled_by` verbatim instead — `gate cognition.janitor.cluster.enabled is not true in config.json` rather than a bare `DISABLED` — and the producer-side fix is filed rather than smuggled in.

- **Create a goal from the dashboard, without a composite row (FR-249)** — the briefs write bar can now make a goal and attaches to it on the next click. `create_goal` is a simple row in the frozen delegation map firing `igris_goal_create` **alone**; eight rows, eight tools, eight dispatches.
  - **The rule this brief was expected to break SURVIVED VERBATIM.** *One row is one `gateway.dispatch`* is quoted in `MAINTAINING.md` and `docs/dashboard.md`, and a create-then-attach composite would have been its first exception. The composite was costed and **rejected on three grounds, not on appetite**: it deletes that rule, it sweeps six assertions over the most-asserted frozen object in the tier, and it buys an acceptance criterion that **cannot be tested without a mock** — `igris_edge_create` never verifies the goal exists (`INSERT OR IGNORE` on any `to_id`), so an attach cannot fail on a goal that was just created. Two clicks, not two workflows: the row declares `returns: "goal.goal_id"`, the dispatcher walks that one path, and the client **preselects the goal it just made**.
  - **THE SHIPPED AC-3(a) GUARD STRUCTURALLY REFUSED THIS BRIEF, IN ALL THREE OPTIONS.** `igris_goal_create` declares `required: ['title','outcome']`, and the guard banned `title` from every row's argument surface — **globally, and entity-blind**, so it could not tell `goals.title` from `brief_status.title`. TD-311's invariant is a claim about BRIEFS. The guard now resolves each row's tool to the entity it writes and applies the ban per entity — **a deliberate widening, ratified by the operator rather than noticed in a diff**, and only as good as three clauses, each asserted and each proven to bite by its own deliberate mutation: the classification table is **TOTAL** over the map (an unclassified tool reds, naming the row), it **FAILS CLOSED** (an unclassified tool is reported *and* still checked against the brief ban), and a **planted `brief_status.title` write must still be refused**. Adding a row now means classifying its tool, not merely naming it.
  - **Independently, the wire keys are PREFIXED — two layers, two jobs.** `params.ts`' unknown-key set is GLOBAL, so admitting a bare `title` would stop `title` being refused *by absence* for `set_priority` too. The body says `goal_title` / `goal_outcome` / `goal_project` and the row's `rename` maps them at the boundary, so that file's stated property holds **byte-identically**. A bare `title` at `create_goal` is still a 400, pinned by `G-TR-18` rather than by a comment.
  - **A third target kind, `none` — the first mutation with NO SUBJECT.** Both `ids` and `refs` are refused by name; `requested` is 1 rather than the sum of two empty arrays (without which a successful create reports `requested: 0, applied: 1`); and the anti-vacuity gate that refuses an empty `ids` does not apply and cannot, because a subjectless create is never vacuous.
  - **Partial failure is PREVENTED, and compensation was never on the menu.** The brain registers six goal tools and **none of them deletes**, so a composite could not have rolled back. `G-TR-17` proves the two-request story with **no mock at all**: the create allocates `GL-102` deterministically (`MAX+1` over the fixture's two goals), then the attach hits FR-247's shipped precondition refusal at the `brief_files`-only brief. End state — the goal exists, serves nothing, and nothing was invented into `brief_status`. **A goal with zero serving briefs is an ordinary state**, not the orphan of a broken transaction, and the retry is one idempotent click.
  - **The plan predicted one refusal message; there are two, from two different layers.** An ABSENT `goal_title` never reaches `handleGoalCreate` — the gateway's BR-080 presence walk refuses it first, with its own sentence — while an EMPTY-STRING one satisfies `key in args` and is refused by the handler with a different one. The gate now asserts each branch against the layer that produces it. The 256-character caps are **not mirrored** in this tier either; the brain's message is forwarded verbatim.
  - **Proven in a real browser, and the browser gate gained an arm it had been missing.** `G-BR-18` types a title and an outcome, reads the new goal back from `/api/goals` **out of band**, asserts the picker is left pointing at it, and then attaches — witnessed by that goal's OWN `serving_briefs` list rather than by any count. Two mutations bite it. Separately, `br12-form-on-board` arms the third clause of `G-BR-12f`'s DOM half: `forms === 0` had been satisfied by a page that rendered no form anywhere, and FR-249 is the brief that put a create control on this surface.
  - **It fits without touching either ceiling.** INITIAL **+0 B** — the type-only widening of `lib/api.ts` emitted nothing, predicted and then measured rather than assumed. TOTAL **+1,704 B** against 15,305 B of slack, split `Layers` +1,212 and `useQFilter` +492 — the second is where `triage/**` living in the shared chunk becomes visible. `MEASURED_INITIAL` / `MEASURED_TOTAL` / `HEADROOM` are untouched: that budget is deliberately CUMULATIVE, and re-basing it would convert it into a per-brief reset. No new endpoint, so the seventeen-GET-and-one-POST exact-set string is byte-identical, and no route became eager or lazy, so `MAINTAINING.md` row 111's re-measure trigger did not fire.

  - **Review caught a FALSE INVARIANT in the shipped wire contract, and it was a mirror pair.** `types.ts` and `lib/api.ts` both said *"exactly one of `id`/`ref` is populated"* — three lines above the `created_id` field this brief added. A `target: "none"` row has NO subject, so BOTH are null, and a renderer following that sentence gets nothing. `types.ts` ships in the npm package, so it was a false claim in the published contract; the tree already contradicted it (`triage/model.ts` renders `"?"` as the honest label). Corrected to "at most one", in both copies, with the reason. No runtime defect — the client already handled it.
  - **Three cardinality claims in hunks this commit itself edited.** `routes.ts` said "both address kinds … all four [extras] to either dispatcher" where there are now three, seven and three; the browser gate's own printed note said `12f` has TWO mutations after this brief registered a third; and two suites said "four domain tables" / "two builders" where there are six and three. All in the class the sweep was supposed to cover, and the eighth consecutive round in which named sites were hit instead of the class.
  - **The diagnostic that measures the budget was itself inflating it.** `DASH_BUNDLE_REPORT=1` writes `dist/dashboard/.bundle-report.json`, and `files` ships `dist` wholesale, so a pack taken right after a report-enabled build read **801 entries / 1,898,453 B** against the comparable **800 / 1,895,163** — a **3,395 B phantom** that looks exactly like code growth. The entry count is the tell, which is the argument for recording entries beside every byte figure. Now guarded by `no build DIAGNOSTIC reaches the tarball`, proven red-first by rebuilding WITH the flag and watching it fire.
  - **Three browser gates went red that this brief cannot have touched — and the baseline says so.** `G-BR-11`'s `11a`/`11b`/`11e` read **158/219 = 72.1%** against a 95% floor, where FR-248's run read **353/354 = 99.7%**. Rather than hunt what FR-249 broke, the previous commit was rebuilt in a scratch worktree and run: it fails there too (**144/203**). Same code, same fixture population (`710 NODES · 352 EDGES` printed in every run), same canvas box — so the `d3-force` layout settled differently, and the reading is coupled to MACHINE LOAD. That is worse than a timeout, because it produces a plausible wrong number instead of announcing itself. Filed as **TD-376**; FR-249 is not its cause. Second time in two briefs that build-the-previous-commit-in-a-worktree turned a suspected regression into an unrelated finding, and both times it cost one run.
- **One search box over the whole brain, fused with RRF (FR-248)** — the operator's 2026-07-31 request was *"you should be able to search anything in the brain, not only learnings."* FR-246 gave every layer its own box; this is the second half: **`GET /api/search`**, one ranked list across briefs, learnings, goals, context docs and suggestions, plus a fifth lazy route `#/search`. Seventeen GET endpoints now, not sixteen.
  - **A DEAD LAYER IS REPORTED, NEVER ABSENT — and that is the whole design.** A fused list silently missing a layer looks exactly like a fused list with fewer matches; nothing on the page distinguishes *"briefs had nothing to say"* from *"briefs was never asked."* So the contract is not *report the error* but **`layers[]` always carries all five entries**, which makes a silent drop **unrepresentable** rather than merely untested. Enforced in a real browser by **`G-BR-17`** against a **7th world `nofts`** where `briefs_fts` is absent — a genuine production state (a brain that never ran v23), seeded that way rather than `DROP TABLE`d after, which would strand v23's six triggers in a state no real brain is ever in.
  - **Two mutations, each shown to redden it and to name it.** `br17-silent-layer-drop` (filter unavailable layers out) reds `17a`/`17b`/`17c`; `br17-layer-lies-available` (a dead layer claims `available:true`) reds `17b`/`17c` while `17a` correctly still passes — the entry is lying, not missing. The server-side twin reds **9 of 42** vitest assertions including *INVARIANT 1*. `routes.ts` has **two** `layers:` assembly points and invariant 1 needs both; mutating only the main one leaves the degraded path green.
  - **`17c` cannot use a naive substring, and that is not a detail.** The retrieval banner on the `nofts` page already contains the literal `briefs_fts`, so "a loud element mentions briefs" would have passed **under the silent-drop mutation**. The check matches `/\bBRIEFS\b/i` — which does not match `briefs_fts`, since `_` is a word character — plus `/INCOMPLETE/i`. A check that passes while the thing it guards is broken is the failure this whole goal kept finding.
  - **THREE OF THE FIVE LAYERS ARE SUBSTRING-ONLY, and labelling them was judged insufficient.** Goals, suggestions and context docs return *the newest match*, not *the best*, so their rank — the input RRF consumes — is meaningless. The operator chose to fuse all five and label loudly over sectioning them off: `rank_basis` rides the layer block AND every row, and the page carries a **mandatory readout** naming how many layers contributed by recency rather than relevance. It reads `fusion.substring_layers` and `layers[].rank_basis` rather than hard-coding which three, and its denominator is `layers.length`, pinned by a six-layer fixture.
  - **`rrf_k` could not be "inherited from FR-246", because FR-246 has none.** The brief said the fusion parameters were inherited and unchanged; reading the code, `memory-read.ts` and `briefs-read.ts` own only INTRA-layer weights (BM25 vs vector). There is no inter-layer `k` anywhere. So `rrf_k = 60` with uniform weight 1.0 is a **new decision**, recorded as one, and echoed in a `fusion` block kept distinct from each layer's own `retrieval.rrf_k` so the two are never read as the same number.
  - **The fused key needs the project segment, and BR-078 is why.** `briefs_fts` indexes `brief_id`, and `BR-001` exists in two projects — `?q=BR-001` really does return both. `layer:id` collides and a UI keyed on it renders one row where there are two. The key is `layer:project:id`; dropping the segment reds five tests.
  - **`?layers=` exclusion is not a fault, and the wire says so.** `LayerReport` carries `requested` alongside `available`, so a narrowed search renders dimmed as *excluded by you* rather than in the fault register. Reading `available` first instead reds four tests. Related trap found by reading `parseLayers`: an empty or all-unknown `?layers=` resolves to **all five, not narrowed**, so the client refuses to remove the last chip — otherwise narrowing to nothing would silently un-narrow.
  - **It fits inside the ceilings without touching either.** INITIAL +299 B (the estimate said ~300), TOTAL +8,695 B, packed **~23.3 KB** against a ~26 KB estimate. The exact byte count lives in `tarball.test.ts`'s ledger row and deliberately not here: this entry SHIPS, so writing the figure changes it, and the last two digits chase themselves. Measured last, then rounded on purpose. `Search` and `SearchReadout` are both DEFERRED, proven from `.bundle-report.json`'s `isDynamicEntry`/`isEntry` cross-checked against `index.html`'s single initial reference. `dashboard-chunks.test.ts`'s operands are untouched: `HEADROOM` is deliberately "≈ four briefs" of CUMULATIVE budget, so re-basing `MEASURED_TOTAL` would have converted it into a per-brief reset — the "raise it in response to a failing assertion" shape that file forbids.
  - **Rollup re-partitioned and the plan's verification became unsatisfiable.** §8.2 said `SearchReadout` "already sits in the shared `useQFilter` chunk … a third async importer adds no duplicate bytes" and §9 said to verify it "stays there". Nothing duplicated, but it did not stay: `RecordList` + `FilterBar` + `SearchReadout` moved OUT into a new shared chunk and the bundle is now **9 chunks, not 8**. Correct Rollup behaviour for a changed sharing set — net +948 B — but the check as written could not be run.
  - **`G-BR-15` was NOT broken by the split, and the real breakage was invisible.** The plan predicted the re-partition would redden it; measured first, it was green — `15d` only asserts the chunk set for `#/overview` and `#/graph`, neither of which pulls the new chunks. What phase 5 actually broke was **`APP_ROUTES`**, a hand-written mirror of `router.tsx#parse`: `search` was missing, so `routeOf("#/search")` answered `"overview"` and would have polled 45 s for the wrong marker — and **no gate visited `#/search`**, so nothing could go red. Fixed by adding `15-mirror`, which compares the mirror's prediction to the `data-route` the shell commits to, per route (`9/9`), plus `#/search` as a ninth route-readiness target and a `15d-search` whose expectation is enumerated from the built artifact's own imports. Its negative half is the point: `Layers` and `Triage` fetch `useQFilter`, and the search route must not inherit it.
  - **The world count is now DERIVED.** `WORLDS` is the single declaration; the banner prints `Object.keys(WORLDS).length`. The file header states no number and no list, because the hand-written count had drifted twice — stale at FOUR, then reading "SIX" beside a list of four.
  - **A pre-existing unowned RED was diagnosed for free, and deliberately not fixed.** `G-BR-4 4c-click` fails on `develop` and has since before this brief (the ladder went 107/109 to 115/117 with the **same two failures**). Cause measured, not guessed: `clickPoint (558, 867)` against `viewport 1440x813` — `pointInsideViewport false`, `elementAtPoint null`. The click lands **54 px below the fold** because FR-250 doubled the canvas from 423 px to 1084 px, so the check reports the FR-239 dead-canvas signature for a canvas `4c-live` proves is alive. That settles TD-352's first acceptance criterion as **candidate (b), harness targeting drift** — the product is fine. Not fixed here: the remedy is an explicit `setViewport` that re-calibrates `4a`/`4b`/`4c-rest` too, which is a brief, not a drive-by. **No `KNOWN_RED` row was added** — that mechanism is for a failure accepted by decision, and declaring a fixable bug would convert it into a carried one, which is the inversion TD-352 exists to prevent.

- **The packed ceiling is raised to +150 KB, and the baseline it measures from was REPLACED rather than corrected (TD-374)** — operator grant, 2026-08-10, taken on a measurement and before the work that needs it. GL-006 could not finish inside the old ceiling: TD-373's clean reading left **1,631 B**, and FR-248 alone needs ~26 KB.
  - **THE BRIEF'S OWN PREMISE DID NOT SURVIVE MEASUREMENT.** `tarball.test.ts` had asserted for weeks that `PACK_BASELINE_PACKED` was "~24 KB HIGHER than a clean baseline" and instructed a re-measure once dist-cleaning landed. TD-373 landed it, so the measurement was taken — FR-238's own commit `71abaa0`, built clean in a scratch worktree: **1,467,162 / 740 files**, against a predicted ~1,277,864 / 715. **+189 KB in the opposite direction**, on a near-identical file count, so it is content and not cruft.
  - **The confound is dependencies, not sources.** `package-lock.json` has drifted since 2026-07-29 (`4ee5ea47b676` → `9ac1bf7d8f75`) and the dashboard bundle is whatever `vite`/`react`/`force-graph` are installed; FR-238's own brain `dist` is *smaller* than today's (4,308 KB vs 4,864 KB), which is ordinary source growth. So **"the clean FR-238 baseline" is not a number, it is a number per dependency tree** — recovering July's needs a period-accurate `npm ci` to describe a July fact that governs nothing. The constant was retired because the question it answers is unanswerable and, once answered, useless.
  - **So the baseline is now a measured present: 1,863,420, clean, on `bd49525`,** with the command recorded. `delta` changes MEANING as well as value — growth-since-clean, not growth-since-FR-238 — and **+150 KB is LARGER than the +550 KB it replaces** because it is measured from a much later point. The absolute cap moves **1,865,051 → 2,017,020, +148 KB of real room**. Every older "cumulative delta" in the ledger converts with a single offset (`new = old − 561,569`) and is flagged as narrative rather than comparable, since all were measured on trees carrying the orphans TD-373 deleted.
  - **The ceiling is DERIVED, not chosen round:** FR-248 ~26 KB + FR-249 ~20 KB + BR-083 ~10 KB + four follow-ups ~5 KB + ~24 KB of changelog and docstring overhead ≈ **85 KB**, so +150 KB is ~76% margin. The overhead line is not padding — this session measured 2.5–4.4 KB per brief, and prose in a shipped file is charged twice via `.js` and `.js.map`.
  - **What the grant does not license.** The instruction inverts rather than disappears: a brief that runs out still cuts scope or vendors less. Two things keep this from being precedent for raising whenever it binds — it was taken BEFORE the work on a measurement rather than after a failing assertion (the standard TD-329 set, and which FR-239 honoured by proposing a raise, measuring, not needing it, and RESTORING the old number), and **it was spent on features rather than leftovers**, because TD-373 deleted 9,624 B of output for deleted sources first, deliberately. Recorded in `MAINTAINING.md` beside TD-329's grant, because a raise the next reader cannot audit is not a decision, it is a drift.
  - **Third time in one session that a long-quoted number turned out never to have been measured** — this note's ~24 KB (real: wrong sign), the ~104.9 KB two separate live directives had been telling planners to read, and — with a caveat in its favour — FR-248's 52,099 B slack bound (real: 1,631 B), which its own plan labelled **"an UPPER BOUND, not a reading"** and flagged as near-certain to be wrong. The plan asked for the measurement; nobody took it for three briefs. The failure was the taking, not the writing. **A number carried in prose across briefs is an estimate until someone re-runs the command.** Here that cost four minutes.

- **The build now prunes, and the running brain is no longer six days old (TD-373)** — `cli/dist/brain-mcp-server/dist/index.js` is the path `~/.claude.json` registers as the `igris-brain` MCP command. It was last compiled **2026-08-04**: TD-327's cognition roster had shipped four days earlier and `roster.js` had never been built, so the live brain could not serve it. Nothing failed, because nothing was asking — this repo had a freshness guard on the surface a USER sees (`dist/dashboard`, TD-276) and none on the surface an AGENT EXECUTES.
  - **The packed ceiling was RED, by three bytes, for a reason unrelated to any recent commit.** `tarball.test.ts` read `563203` against a `563200` ceiling. The cause was not code that grew: `tsc` emits but never prunes, so **24 artifacts whose sources `c6777bc` deleted** — `subconscious/detectors/{conflict,gap,pattern,stalled}` plus `readonly-db` and `verifier` — had been shipping in the npm tarball ever since, worth 9,624 B. Deleting a source makes nothing newer, so **every mtime staleness check reports fresh, forever.** `copy-templates.sh` had exactly such a check and it had been passing correctly and uselessly.
  - **Two defects need two assertions, and that is the whole lesson.** Freshness is a property of what is THERE; orphaning is a property of what is missing from the OTHER side. `brain-artifact.test.ts` (NEW) asserts both, on both copies — `brain-mcp-server/dist` (what `tsc` writes) and `cli/dist/brain-mcp-server/dist` (what ships). Not because the stage is additive (it is not; `copy-templates.sh` does `rm -rf "$MCP_DEST"` first) but because **the two are cleaned by different commands with different triggers**: `cd brain-mcp-server && npm run build` — which CI, the VPS deploy script and `igris sync code` all run on its own — prunes upstream and leaves the SHIPPED copy holding the orphan it just deleted. Demonstrated, not argued: plant one in both, run only the brain build, and the upstream assertion goes GREEN while the shipped one fires. The shipped copy is what `npm pack` reads and what the ceiling charges for, so asserting only upstream would go green on the exact state that ships the bytes. Each assertion proven red-first with its own planted mutation, and each fires alone. It asserts the RELATION (every emitted file traces to a source), never a file COUNT — a count goes green on a delete-plus-add and needs re-blessing on every legitimate module. Same shape as TD-372's `NEVER_DEGRADES` finding one week earlier.
  - **`copy-templates.sh` gained the orphan scan too**, so the build self-heals rather than only reporting: it rebuilds when `dist/` is absent, when `src/` is newer, **or when `dist/` holds output for a deleted source**. Red-first — a planted `ghost.js` prints `dist/ holds output for a deleted source (engine/components/zzz-orphan/ghost.js)` and is gone after the build; the negative control confirms a clean tree does NOT rebuild, so the scan cannot make every build unconditional.
  - **Both builds now clean, in DIFFERENT shapes, because they have different callers.** `cli` leads with `rm -rf dist`. `brain-mcp-server` builds to `dist.tmp` and swaps — because `igris sync code` and `scripts/igris_brain_deploy.sh` run its build **on the VPS while PM2 `igris-brain` is still serving**, and that path's documented invariant is *"fail loud BEFORE we tear down the running brain … old brain stays serving"*. An in-place clean would delete the last-good artifact before knowing the new one compiles, so a failed build plus any later PM2 restart would find no `dist/index.js`. Verified by planting a type error: the build exits 2 and `dist/index.js` is byte-identical afterwards. `build-dashboard.sh` had argued for unconditional cleaning in its own header ("a skip-if-present branch is exactly how a stale bundle ships to npm"); the compiled halves had never adopted it.
  - **A clean build revealed the `igris` bin was executable only by historical accident.** `tsc` emits 0644 and nothing in the build ever set the bit; it worked because some past `npm install` chmod'd the file that existed at that moment. The first `rm -rf dist` produced a `dist/index.js` that could not run, and three `harness-registry` assertions failed with `Permission denied` from a shell two layers down — a failure that reads as a compiler bug and is a file mode. `chmod +x` now runs in the build, with the bit and the shebang both asserted, red-first.
  - **The pack ledger got its row, which is the part that actually reaches the next planner.** `tarball.test.ts` carries the measure-and-record convention every brief since FR-238 has followed, plus two standing instructions addressed to "the next real build" — BR-085's and TD-347's un-rebuilt FLOORS. TD-373 is that build. Its row records the first CLEAN reading the ledger has ever held (**1,863,420 / 796 entries / +1,631 B, PROVISIONAL**), the 9,624 B of orphans deleted, the measurement technique so a fourth brief does not re-derive it, and a deliberate refusal to isolate its own share: the 5,541 B between this and a clean `HEAD` is TD-373's own `cli/CHANGELOG.md` entry **and** a concurrent session's TD-367 work in the same file, and separating them would mean stashing another agent's live work. (An earlier draft attributed all 5,541 B to the other session and forgot its own entry — the exact over-attribution TD-333 warns about, committed while citing TD-333.) The directive at the ledger's head, **"~104.9 KB is what is left"**, is re-pointed — it was 26x generous, and every superseded figure in that list was measured on a tree carrying orphans. **(That sentence was wrong about its own scope, and TD-374's review caught it: there are TWO such directives in the file and this pass hit one. The second is corrected in TD-374 and labelled as the second copy. Sweeping the named site instead of the class, in the entry claiming to have swept a class.)**
  - **THE MEASUREMENT IS THE POINT.** FR-248's plan bounded the packed slack at **52,099 B** and the operator chose to take a real reading in a scratch `git worktree` rather than ship on a floor for the third consecutive brief (`tarball.test.ts` records BR-085 and TD-347 doing exactly that). The measurement was **9,621 B** at `HEAD` — five times tighter, and the working tree was already over. FR-248 was scoped against the estimate and does not fit the measurement; it is **Blocked** pending a re-scope. Cost of the reading: one worktree, four minutes.

- **The dashboard's visual grammar has a home, and mutation testing found which half of it is naked (TD-335)** — `design_system.md` was applicable-and-absent through five dashboard briefs (FR-239/240/244/245/247), each of which declined it for a good local reason. What changed is that the grammar became load-bearing while its only statement was a CSS block comment beside its first use, and **shipped files already asserted the doc existed** — claims written ahead of the filing. Grounded at `~/.igris/projects/igris-ai/context/design_system.md` by CONSOLIDATING what shipped, never by inventing.
  - **THE INVARIANT IS SCOPED TO NODES, and getting that wrong is what two review rounds were spent on.** *For NODES: shape encodes ROLE, colour encodes STATE.* **For EDGES the law inverts** — `edges.ts#roleColour` maps each of the four edge roles onto a palette token, the dash carries `optional` (`edges.test.ts` T20: *"the dash survives the dimming — it IS the role"*), and `tier.ts#NEVER_DEGRADES` keeps both alive at every tier while opacity does the degrading. Stating *"colour encodes state"* unqualified is false about half the canvas. The edge vocabulary was **missing from the consolidation entirely** and is now harvested with all four of its rules and their enforcing tests.
  - **The invariant leads the doc — and the first draft was wrong about why.** *Shape encodes ROLE; colour encodes STATE; at `silhouette` chrome shape is 100% of the role signal.* The draft claimed nothing could test it and a change erasing role encoding would keep every suite green. **Validation broke the rule and watched:** erasing the shape→role map fails `shapes.test.ts` "maps D9 exactly", and rounding every node fails three more assertions there. Only the **colour** half is genuinely unguarded — colouring nodes by TYPE instead of by interaction state was applied as a mutation and 588/588 stayed green. The closing table now carries the real enforcer for every row, each verified by mutation rather than asserted, and says plainly that shape=role is defended by tests while colour=state is defended only by the page. A doc that overstates how naked a rule is teaches the reader to distrust the rest of it.
  - **Two rules were mislabelled, both in the direction that gets someone hurt.** The 22-character board truncation shipped as "convention only"; it is `LABEL_MAX` in `layers/board.ts`, **pinned to 22 by `record.test.tsx` R2** (`22 → 30` turns it red). And rule 1 — "every colour dereferences a role token" — is enforced only as *no literal colour SYNTAX*: the scan matches `#hex` and `rgb()/hsl()`, so `color: red` passes. Both now carry what is executable and what is intent, plus the second enforcer (`dashboard-graph-source.test.ts`) the draft never named.
  - **The one named divergence from the external brand book is not a divergence.** The draft recorded `EXTERNAL`-not-`EDGE` as a local deviation. `shapes.ts` says the opposite in its own docblock: **`dataviz.md` §02 makes the rename normative** — same shape, same treatment, a sanctioned name. Corrected, with the real deviation list left where it lives (`PORTING.md` D1–D15) rather than duplicated.
  - **Five roles are four silhouettes at Tier C, and nothing measures it.** `TOOL` traces the same rectangle as `SERVICE`; the dash that separates them is applied in the branch `silhouette` skips, and size is role-independent. So an `error` node is pixel-identical to a brief in the exact view where the doc says shape is the only role signal. Recorded as the strongest evidence *for* the invariant rather than smoothed away.
  - **The FR-244 near-miss is recorded with its outcome, and it turns out to be enforced.** The brief's first AC asked for circular nodes; rounding everything would have erased role in exactly the view the complaint came from. The request was **withdrawn, not refused**, once the cost was shown — legibility was the GOAL, circles were a MEANS, the means was dropped and the goal pursued through density. Nobody can re-land that AC by accident: the shape-geometry assertions catch it.
  - **THE BRIEF'S OWN CITATION WAS WRONG, and copying it would have seeded a new doc with a false figure.** TD-335 instructed recording the near-miss "with the measurement that settled it (only 41% of `Refactor` rows carried a `TD-` prefix)". The figure is **unsourceable anywhere in the corpus** — no brief but TD-335 itself contains `41%`, no learning does, FR-244 contains neither the figure nor the word `Refactor`, and TD-328 (the nearest subject match) records the opposite position. An earlier draft of this entry attributed it to TD-328; that attribution was itself unverified, which is the same defect one layer up. What settled FR-244 is a chrome analysis, not a percentage. The figure is absent from the doc.
  - **Eleven shipped claims were made true, across five files — the brief named two.** `base.css` (four blocks: FR-239, FR-240, FR-245, FR-247), `RecordBoard.tsx`, `PORTING.md` (its opening and the whole of §6, now past-tense with the provenance/convention split stated), and `browser-gate.mjs`'s `K_FLOOR` docblock. The CSS blocks are reframed as the grammar's ORIGIN rather than its home, with an explicit tiebreak: if the two disagree, the CSS ships and the doc is stale. The twelfth site is a TEST comment, and its assertion is **retained deliberately** — the doc is runtime-only with no repo mirror, so nothing in this repo can test it, and that check is what keeps the grammar stated beside the code if the doc is ever lost. Sweeping the named sites instead of the class is a failure this repo has now logged four times; validation caught the fifth.
  - **THE MIRROR MISTAKE, caught in round 3: the edge law is not the node law inverted.** Round 2's fix said edges spend colour on role and opacity on state — tidy, and refuted by `edges.ts#restingColour` in the file it cites. `roleColour` is a pure role→token function and never degrades, but the colour actually DRAWN is tier-dependent: at Tier B `data` takes the **`muted` token, which is `control`'s own role colour**; at Tier C `data` and `control` both become `edge-dim`. So four edge roles render in two colour buckets at Tier C — the edge analogue of the node-side `TOOL` ≡ `SERVICE` collapse, in the same view, and `edges.ts` says so itself: *"the colour role is what rung 3 is allowed to take."* The **dash** is the carrier that survives every tier, which is what makes `edges.test.ts` T20 load-bearing. State on an edge has three carriers (width, arrowhead presence, alpha), not one. The doc now refuses the tidy summary in writing: at full chrome both vocabularies are rich; at Tier C each is down to one geometric carrier, and **the collapse is unmeasured on both sides**.
  - **A row labelled `executable` on a test that does not assert what it names — filed as TD-372.** `tier.ts#NEVER_DEGRADES` has five members; `tier.test.ts` names three and then loops the array itself, so a *deleted* member cannot fail it. Deleting `"role-semantics"` — the entry `edges.ts` cites as the reason role colour survives dimming — leaves the suite green at 21/21. The doc's row is now PARTIAL with the mutation recorded.
  - **The harvest was still incomplete after two rounds, in the same direction both times.** Round 2 found `edges.ts` missing from a doc claiming to consolidate the canvas grammar. Round 3 found four more: the **four easings** (enforced in the *same assertion* as the durations, whose title says "and four easings"), the **8-pt `--s-*` scale** (`tier.test.ts` T18), the **3-tier type stack** and **four palettes**, plus two whole vocabularies — `graph/labels.ts` (mono/uppercase, a 28-char cap distinct from the board's 22, right→left→below→above placement, and the load-bearing *contact counts as overlap* strict-inequality rule that makes an unplaceable label **not drawn**) and `shapes.ts#GLYPH` (whose `⌁` and `//` are local inventions against a spec naming only three marks, with **no D-record in `PORTING.md`** — TD-369). `PORTING.md` §6's banner had asserted the doc carried all seven token families while it held three; the harvest was completed rather than the claim softened.
  - **THE FOURTH ROUND FOUND THE FOURTH SCOPE ERROR, in round 3's own headline fix.** Round 3 wrote that at Tier C each vocabulary is down to one carrier "and the collapse is unmeasured on both sides" — true of nodes, **false of edges**: `edges.test.ts` *"Tier C dims resting data and control to the derived alias"* pins that collapse deliberately. Round 3 had taken a verified node-side statement and generalised it across two element kinds without re-checking the second. The doc now carries a four-row comparison table instead of a summary sentence, and states the asymmetry that is the actually useful part: **the edge collapse was designed, so it got a test; the node collapse is a consequence of `TOOL` and `SERVICE` sharing a path, so nobody wrote one.**
  - **Width is a STATE carrier, and the source comment that says otherwise is wrong for half the roles it names.** `edges.ts` justifies rung 3 taking the colour role with *"`optional` and `control` keep their identity through the dash and the width"*. True of `optional`, which is dashed. **False of `control`**, which is neither dashed nor wider — width keys on `activity`, never on role. So at Tier C rest a `data` edge and a `control` edge are identical in **every field of `EdgeStyle`**, a total collapse rather than the "two colour buckets" an earlier draft settled for. Recorded, not repeated → **TD-371**.
  - **A SEVEN-ROW MUTATION SWEEP, and it moved seven labels off what a careful reading gave them.** Rows that survived: `roleColour` (control → bone reds 3), `restingColour` at both tiers, the `optional` dash (reds T20 — an earlier draft said "reds 7", which holds only for the literal `= null` mutation where five of the seven are TypeError crash-reds; the semantic mutation reds 2, both in T20), `floorToken`, the five durations (appending a sixth reds 3), the four easings, label uppercase and placement order, and *contact counts as overlap* (`<` → `<=` reds "touching boxes REJECT"). Rows that did not, and are now marked `nothing` or PARTIAL: **the `--s-*` scale's VALUES** (`8px` → `7px`, all 588 green — only the token NAME is pinned), **`GLYPH` uniqueness** (duplicate a mark, all green), `role-semantics`, node colour-by-type, and `TOOL` ≡ `SERVICE`. Three rows had been labelled `executable` on tests that assert something adjacent to what the row claimed.
  - **The third missing harvest, and the one most likely to mislead: `BRAND_RULES` and the scan's EXEMPTION.** The four CSS constraints hold from the FR-240 marker to EOF — so the ~1,250 lines **before** it are ported brand classes that legitimately carry hex literals (`.status-pill-live` is `#22c55e`; `.badge-alarm` carries `rgba(220,38,38,…)`). Those are SEMANTIC colours: live and alarm mean the same thing in every palette, so they deliberately do not swap. "Zero colour literals" is a rule of the locally-written layers, not of the file, and the doc now says so before someone "fixes" upstream. The **six** named `BRAND_RULES` non-negotiables — numbered 2 through 7, with no #1 in the block or anywhere in the repo, and including `.chip`'s explicit `9999px` exception to the no-radius rule — are recorded as prose provenance enforced by nothing.
  - **THE FIFTH ROUND FOUND THE FIFTH, in the table built to stop the fourth.** Round 4 replaced the summary sentence that kept coming out wrong with a comparison table — and **two of its cells were wrong the same tidy way.** *"What survives Tier C: dash only"* ignored that `optional` and `hot` keep `accent` at 0.38 alpha; the mutation behind the cell had tested `data` and `control`, two roles of four, and `edges.test.ts` *"optional keeps its ACCENT hue even when dimmed"* refutes it directly. And the node column omitted the **dash** — `TOOL` is dashed at `full` and `silhouette-padded` (pinned by `shapes.test.ts` *"only TOOL is dashed"*), which is the exact carrier the row below is about, so the table implied the node collapse begins at Tier B when it begins at `silhouette`. Both cells corrected, with the two failed drafts recorded beneath the table rather than quietly replaced.
  - **A shipped comment falsified by this very commit, in a file this commit already edited.** `shapes.ts`'s `NODE_SIZE_ZOOM_FLOOR` docblock said the k/k_fit sweep *"belongs wherever the design tokens are eventually written down (TD-335)"*. The doc now exists **and explicitly refuses it** — its *Numbers this doc deliberately does not carry* section keeps every measured figure with the artifact that produces it. The commit had corrected the OTHER TD-335 claim in the same file and stopped. Sixth instance of sweeping the named sites instead of the class; the line now says DO NOT MOVE THE SWEEP HERE and records why.
  - **Four smaller false counts, all found by reading the source the doc cites.** `BRAND_RULES` is **six**, numbered 2–7, with no #1 in the block or anywhere in the repo — the doc and both changelogs said seven. Constraint #1's role-token list said `--bg`; the block header it quotes says `--status-*`, which is precisely the semantic family the new exemption section exists to explain. Label placement is **PARTIAL**, not executable: right-first and left-fallback are pinned, nothing distinguishes `below` from `above`. And the `TOOL`-as-a-circle mutation reds T20's **closure** check (`expected 'arc' to be 'closePath'`), not its bounds check — the conclusion held, the parenthetical did not.
  - **A brief-id collision with a concurrent session, resolved by yielding.** Two agents were working one worktree; the TD-367 session had written **TD-368** into its changelog prose for that brief's residual, and this session minted TD-368 for a review finding forty minutes later. The finding **moved to TD-372**; TD-368 is left as an explicit tombstone naming TD-367's residual as its owner, because a silently reused id would make a correct changelog pointer resolve to an unrelated brief. Brief-id allocation is read-then-write with no reservation and the duplicate check fires on CONTENT, not ids — recorded in the tombstone, not filed, since one instance is not a pattern.
  - **A `nothing` cell is a claim about a MUTATION, and a mutation only tests the scope it was written at.** The doc's `nothing` verdict for colour came from a mutation that recoloured **nodes**; it was filed under a sentence about **colour**. Both review rounds hit the same class from opposite sides — round 1 called a defended rule naked, round 2 called a naked rule broader than what was measured. The rule the doc now gives the next author: match the sentence to the mutation, or run a wider one.
  - **A count that contradicted five other sites was fixed in the docblock whose job is to state it.** `browser-gate.mjs`'s `K_FLOOR` block said *"three checks in this file became downstream of a design token"*. It is **four** — `11a`, `11b`, `11e`, `11-range` — per `MAINTAINING.md` row 121, `graph/shapes.ts`, `dashboard-graph-source.test.ts`, the paragraph 48 lines above it, and the design doc it now cites. `three` matched nothing anywhere and understated the blast radius in the one place a reader goes to learn what moving the constant costs. Pre-existing, adjacent, and squarely TD-335's subject, so corrected here rather than re-filed. **The residual is recorded rather than papered over:** the same file 420 lines below, and `docs/dashboard.md`, both say FIVE (adding `11-anchor`). The two senses plausibly differ — `11-anchor` asserts achieved-vs-requested zoom AT each anchor rather than deriving a threshold FROM the floor — but the repo states both counts flatly, and picking between them is TD-337's contract, not a documentation brief's call. Filed as **TD-370** with the criterion that should decide it.
  - **A stale comment inside the branch it describes.** `shapes.ts`'s `silhouette` branch read *"Role is carried by shape plus colour role"* — true of edges, false of nodes, and it sat three lines above the `fill` that proves it. Corrected in place, since the whole point of this brief is that a shipped file stating something false is reject-class.
  - **No figure is copied from the pack ledger, the byte ceilings or the browser gate** — the doc carries a section naming all three owners and states the rule as a rule: a design doc is an especially bad place for a stale number, being read for orientation, rarely re-derived, and executed by nothing. The live node population went the same way — the rationale stays, the number stays in `palette.ts`.


- **`igris remove --help` no longer claims it un-projects from every harness, and the guard that should have caught it now reads the CLI (TD-367)** — the shipped `remove` description said *"UN-PROJECTS from every harness"* while `igris add --help`, **143 lines above it in the same file**, had already been corrected to the per-surface predicate, and both of `remove`'s prose twins (`cli/README.md`, which ships to npm, and `docs/multi-cli.md`) were hedged **in the same diff**. Rebuilding `cli/dist` therefore made it worse: the binary emitted corrected `add` help beside stale `remove` help. Both now name what actually happens — skills and MCP reach every harness with an `agent_id`, agents the entry's own `targets[]`, hooks every harness with `hooks.supported` true — and both carry the `jq` line that re-derives the roster.
  - **The `--harness <type>` option descriptions were three different wrong answers to one question.** `loadout`'s said `claude | codex | gemini | opencode`, `add`'s said the same four, and `remove`'s said those plus `antigravity` — while the validator they all feed accepts **six**. Three shipped, mutually inconsistent enumerations of the same set, recorded nowhere and contradicting the code twenty lines away. All three now name the descriptor and say how the run verb narrows it per surface, so onboarding a harness does not silently falsify three help strings.
  - **The real fix is that the guard scans `cli/src` now, because five hand sweeps had not ended this class.** `scripts/validate_harness_tier_claims.sh` walked `*.md` only, so **no shipped string in the CLI was ever covered** — every recurrence had been in a surface the instrument structurally could not see. The scan unit was **measured, not assumed**: all of `cli/src/**/*.ts` is 203 files / 23 hits, excluding `__tests__/` is 102 files / **12 hits with zero false positives**. The 11 dropped hits are test titles scoping a fixture plus one UNIX FILE MODE (*"a 644 harness config"*), and a "string literals and comments only" unit was measured too and **discarded as worthless** — all 23 hits already sit on a comment-or-quote line, so it separates nothing while adding a TypeScript-lexing step to a grep. The dimension that discriminates is test-vs-shipped, which is a path. Identifiers and enum members, the noise the widening was expected to hit, produced **zero** hits at either unit. Corpus 101 → 203 files, runtime **0.28s → 0.82s** (warm median of 7 samples each).
  - **Every count it reported is fixed, and a comment is not a repo-side note.** **THIS BULLET NO LONGER CARRIES A SITE TALLY.** It said *"23 sites: the 12 the guard reports, plus 11 it structurally cannot"*, and rounds 6 and 7 falsified it twice over — the round-6 adjective widening exposed hits nobody had counted, and round 7 found three more in a notation no arm can see (below). A hand-maintained tally of a guard's own coverage is the identical defect to a hand-written harness count, which is the lesson that had already deleted the summary count from the guard's heading, from limit #6 and from the `MAINTAINING.md` row; restating it here at a new number would just reset the clock. The SHAPES are what transfer, and they are stable: five adjacency misses (including `mcp-register.ts`'s two *"Defaults to all 5"*, where the noun sits 30 characters from the numeral), one LINE WRAP (`verbs/add.ts`'s *"across all 4 / harness configs"*, the same blind spot that survived an earlier round in `docs/multi-cli.md`, recurring in the new tree on its first scan), and five non-numeric. `mcp-register.ts` carried **six** stale counts, not the four the deferred residual claimed. Also corrected: `harness-descriptor.ts`'s *"The five Igris harness SHAPE ids"* above a union of **six**, `mcp-shape.ts`'s `@param` *"the other three harnesses"* — **and only that one; its noun-elided twin seven lines above shipped on for another round, which is finding F4 below** — `skills-delegate.ts`'s two *"5-harness smoke gate"* references (now named by their brief, FR-212d, since a gate is identified by what ran it and not by how many harnesses existed that week), `loadout.ts`'s *"SEPARATE 4-harness enum"* (the const it names was deleted by FR-217 M5), and `verbs/add.ts`'s doc-comment twin plus its *"across all 4 harness configs"*. `removeComments` is unset, so each of these was compiled verbatim into `cli/dist/**/*.js` — the thing users actually run.
  - **A prospective blind spot was measured and CLOSED rather than stated.** The digit half rejects a leading hyphen to keep brief ids (`FR-136 Harness`) out of the report, which also made `all-6-harness` — the digit mirror of the `all-four-harness` compound the word half already catches — invisible. The two shapes are separable by their **separator**: a brief id puts a SPACE before the noun, a compound puts a HYPHEN. Measured over the real 203-file corpus **before** it was written: 12 → 12 hits, zero false positives, so it was fixed instead of documented. Paired tests pin both directions, and the residual it accepts (a hyphen-JOINED brief id) is stated rather than hidden.
  - **The ANAPHORIC count — a notation where the numeral is written and the NOUN IS ELIDED — was found by sweep, fixed by hand in three shipped files, and declined as a fourth arm on numbers (round 7, F4).** *"the other N"* is invisible to every arm BY CONSTRUCTION: arm 1 needs the noun adjacent, arm 2 a CLI value grammar, arm 3 a display-name run. All three live instances were in `cli/src`, all off by one, all omitting cursor, and **all in files this same diff was already correcting — two of the three inside the VERY SAME comment block as a line it had just fixed** — `cli/src/lib/mcp-shape.ts` above a switch with **six** cases (whose `@param` twin **seven lines below it** had been corrected in an earlier round while this sentence shipped on, which is why the bullet above now names which of the two was fixed); `cli/src/verbs/loadout.ts` **sixteen lines** under a line this round had corrected to read *"`mcpTargetTypes()` minus the antigravity carve-out"*, i.e. five; and `cli/src/verbs/remove.ts`, **in the same file whose header docblock this diff had just rewritten to stop claiming `remove` reaches every harness** — the flagship correction of this very entry — where the targeted set defaults to `mcpTargetTypes()` and the antigravity filter leaves five. `removeComments` is unset, so all three compiled verbatim into `cli/dist`. Each now states the property instead of a number.
    - **The arm was measured before being declined, on the same terms as arms 2 and 3.** Over the 203-file corpus with the defect in place: the bare anaphor **15 hits / 3 true**; plus a harness-vocabulary discriminator on the SAME line **4 / 2** — and the one it drops is `verbs/remove.ts`, whose comment names no harness at all (*"antigravity"* is on the line above), so the only tolerable-precision unit has a **false negative on a real defect**; plus a context window of ±3 or ±10 lines **8 / 3**. **Re-run after the fixes rather than subtracted** (the error the guard's own limit #8 records making): the best unit still reports **5 with ZERO true** — dashboard endpoints, brain read modules, BR-080 required arrays, portable hook EVENTS, and the `MAINTAINING.md` row describing this guard. It would sit RED on a clean tree forever, need a five-line allowlist, and flag its own decline. Same verdict, same evidence, as limit #8's decline of arm 2's broad predicate.
    - **The CONFIG-PATH shape was swept in the same pass and is CLEAN, which is recorded because looking and finding nothing is a measurement** and the guard's limit #7 had parked it as unmeasured. With the paths derived from each harness's `mcp.config_path`, the presence predicate reports exactly **one** line at both N=4 and N=5 — the 5-axis divergence table in the cli-adapters README, correct as written because `cursor` appears **zero** times in that bash adapter. Zero true positives; nothing to build.
    - **A FOURTH instance turned up in a WHOLE-TREE sweep of the class, outside the three the review named — and it is DOUBLY invisible.** `cli/src/__tests__/harness-registry.test.ts` said the universal skills store serves *"the other 4 / harnesses"*; every declared harness carries an `agent_id`, so it serves **five**. The guard could never report it for **two independent reasons at once**: the noun **wrapped to the next line** (limit #2) *inside* the directory the source walk **prunes** (limit #6), so un-pruning alone would not have found it either. It is a doc comment describing PRODUCTION routing, which is precisely the cost limit #6 says the `__tests__` prune accepts — and it is why both that limit and the `MAINTAINING.md` row now say *"every round since this exclusion landed has had to fix one by hand"* instead of carrying a count that goes stale on the next one.
  - **A live mutation survived the arm-3 test suite, and the fix is a test rather than a code change (round 7, F1).** Changing arm 3's distinctness recount from the matched RUN to the whole LINE — one token — leaves the guard green on the real tree, `bash -n` clean, and **every one of the 44 twin assertions passing**. It is a silent FALSE NEGATIVE, and it converts arm 3 into precisely the broad predicate limit #10 measured (7 hits, 6 false) and declined. Neither existing fixture could see it: five copies of one name count 1 distinct at either scope, and the wrapped-enumeration fixture never reaches the recount because its run is four elements and the match gate rejects it first. The new fixture makes a name sit OUTSIDE the run and pins **both** directions in one file — a genuine 5-name run whose sixth name is elsewhere on the line (run-scoped: reported; line-scoped: silent) and a 5-element run with only 4 distinct names plus a fifth elsewhere (run-scoped: quiet; line-scoped: over-fires). Root bats **618 → 619**.
  - **The arm-3 threshold's justification was FALSE, and only the justification — the threshold stays 5 (round 7, F2/F3; the sweep finished in round 8).** **AND THE RETRACTION'S OWN LIST OF THE SITES WAS SHORT, TWICE.** This bullet said the guard header and the `MAINTAINING.md` row *"both"* claimed it. There was a **third** — the bats twin `test/validate_harness_tier_claims.test.bash`, whose threshold test opened with the same sentence as live justification and which survived a round *in which that very comment block was edited*, so the guard header called the claim FALSE while its own twin went on asserting it. Re-derived rather than recalled, the corrected list was short **again**: the guard's own file asserts it in two more places (the `TIER_DISPLAY_MIN` env-var doc and the arm-3 preamble above the default), as did the arm-3 adoption bullet in the ROOT `CHANGELOG.md`'s TD-367 entry — not this one, which carries no arm-adoption bullet. The sentence was true where it was written and false where it was copied here, which is mirror-pair drift inside the retraction for a mirror-pair claim. **Four sites survived a retraction believed complete, two of them inside the file carrying it.** A hand-written enumeration of the sites bearing a false claim is the same defect as a hand-written harness count, so it is replaced by the property plus the command that re-derives it — grep the CONCEPT, not the phrase, and adjudicate each hit as ASSERTS or RETRACTS: `grep -rniE 'strictly above every declared|property of the descriptor|number that happened to work|collides with correct per-surface' . --exclude-dir=node_modules --exclude-dir=.git`. Retracting sites quote the false sentence **on one line on purpose**: the twin's first retraction draft wrapped it and vanished from that command, which is the guard's own limit #2 recurring inside the fix for it. The claim was that five *"sits strictly above every declared subset"*. It does not: `mcp.projected` is a **declared five-member proper subset** (antigravity is the FR-179 carve-out), read by the shipped exported `mcpProjectedHarnesses()` in `cli/src/lib/harness-descriptor.ts`, whose own doc comment names the predicate. So the threshold sits **on** a declared subset. What actually justifies it is empirical and was re-measured over the repaired tree: **N=5 → 0 hits, N=4 → 5 hits and all five correct** (an evidence claim in `README.md`, two wrapped per-surface subsets in `docs/multi-cli.md`, the `MAINTAINING.md` row itself, and a gloss that already names its accessor). No 4-name roster in this corpus should be caught, so 4 buys nothing. Both artifacts now say empirical, which is the **weaker** guarantee — it is unverified when the roster changes *and* when the corpus changes.
    - **The five-member residual was declared non-existent, *"measured, not assumed"*, and it was neither.** Two lines in `docs/multi-cli.md` enumerate a five-member subset in display names right now and **both are correct sentences the guard would report if it could reach them**: the `${VAR}` indirection rule (every harness except codex resolves the ref itself, which is **five** — the guard's own limit #10 called them *"the four harnesses that resolve a ref themselves"*, a stale roster count inside the paragraph documenting stale roster counts) and the delegated-set parenthetical. **Neither is cleared by the threshold.** The first is cleared by the LINE WRAP alone — joined onto one line it reports, measured rather than reasoned. The second needs **two** accidents: the wrap *and* an interposed *"— since FR-192 —"* that breaks contiguity; joined verbatim it stays quiet, joined with the parenthetical removed it reports. Arm 3's clean run is therefore carried by line wrapping, which the guard's limit #2 already names as a thing that moves whenever a paragraph is re-flowed. The bats fixture that pinned the threshold made the same mistake and is corrected: it used the `${VAR}` set and called it four, so it asserted the threshold's justification with a stale count. It now uses the `agents` block, whose size is declared and re-derivable with one `jq` line.
  - **The fourth stale tally in the guard's own file is deleted, not corrected (round 7, F5).** `DEFAULT_SRC_EXCLUDE`'s comment hardcoded three figures — a corpus delta, a walk delta and a breakdown of the hits the prune drops — and by round 7 **all three were wrong at once**: the corpus is 203 → **305** (not 304), the walk 102 → **204** (not 203), and the dropped hits are **10 = 8 test titles + 2 comments** (not 11 = 8 + 3). Deleted and replaced by the derivation command the guard already ships, exactly as the heading's count of its limits, limit #6's shape list and the `MAINTAINING.md` row were. **The row also asserted a removal that had not happened yet** — it said *"the guard's own header removed the same figure for the same reason"* while the header still carried all three; that sentence is corrected and now names what the header actually does (limit #6's remaining figures are dated measurements explicitly labelled as history).

- **A brief can no longer close through `/hunt` with an unticked acceptance criterion (TD-325)** — the `gap` detector had reported this faithfully since 2026-04-29 and it was read as noise for twelve weeks. **396 pending gap rows on `igris-ai`**, unchanged between 2026-07-31 and 2026-08-07 — and **734 across all projects**, which is a different scope and not a growth figure. (An earlier draft of this entry compared the two and reported the queue as having 'nearly doubled'. It had not; the igris-ai gap count never moved. Recorded because reading a number without checking its scope is the founding failure this brief exists to fix, and it was committed while documenting it.) The operator's diagnosis was the correct one: the briefs are genuinely complete, the boxes just were not ticked. So the detector was working and the process was not, and dismissing the suggestions would have retired the symptom.
  - **DEMONSTRATED ON THE COMMITS THAT SHIPPED THE SAME DAY.** The four briefs closed on 2026-08-06/07 — TD-347, TD-330, TD-331, TD-327 — are all `Done`/`COMPLETE` with **zero** ticked criteria. They are the gate's RED fixtures, byte-unmodified, and the gate refuses all four. `FR-241` is the GREEN fixture: it already used `- [~]` + `DEFERRED`, invented before there was a rule for it. **TD-325's own closing commit was refused by the gate it installs** until its seven criteria were resolved — the end-to-end proof.
  - **Neither obvious surface could host it.** `igris_brief_sync` cannot refuse: `/hunt` Phase 7 orders phase=COMMITTING → `git commit` → status=Done → sync, so BOTH terminal syncs are post-commit. Rejecting on `status` leaves a landed commit with the store saying open (C3 committed-but-open); rejecting on `phase` manufactures C1 Done-but-not-COMPLETE, which TD-311 then forbids fixing by editing brief data — trapping the operator. The pre-commit phase guard cannot host it either: `/hunt` COMMITTING sets `IGRIS_BYPASS_PHASE_GUARD` on the exact commit that needs gating. It lives in **`commit-msg`**, keyed on the `closes #<ID>` footer — already the repo's own definition of a closing commit, so it needs no phase heuristic and has no bypass entanglement. The brain gets the OBSERVER role instead, matching the shipped `nonCanonical*Note` pattern.
  - **HARD-FAIL, deliberately the brief-state family's first blocker** (operator decision D3). Every sibling check is WARN-only, and twelve weeks of WARN is what produced the backlog. One-shot `IGRIS_BYPASS_AC_GATE=1`.
  - **THREE DEFECTS FOUND BY MEASURING THE CORPUS, NOT BY TESTING FIXTURES.** (a) **Ordered markers**: 9 briefs write criteria as `1. [ ]` from the v4-era template, with no `- [ ]` substring anywhere. A hyphen-only regex reads **37 briefs differently and turns 32 of them from FAIL into `total=0`** — a green verdict earned by parsing nothing. *(Precision, per validation: under the shipped parser those 32 land on `NO_ITEMS`, not `PASS`; reproducing the literal `PASS total=0` needs both the marker fold reverted AND the `NO_ITEMS` branch deleted. Both exit 0, so the gate consequence is identical and only the label differs — recorded because the distinction is the kind that gets overstated.)* (b) **Heading anchor**: `^acceptance` missed `## Epic-level acceptance criteria`. (c) **Multiple AC blocks**: MG-013 read 18 of 30 criteria and FR-120 read the wrong block.
  - **The population number moved the OPPOSITE way to the prediction, and both directions are explained.** The plan expected the anchored parse to yield FEWER than `content LIKE '%- [ ]%'`. Over 447 terminal briefs: **LIKE 379, parser 388** — `379 − 2 + 11 = 388`. The 2 LIKE-only are substring false positives (a checkbox inside a prose table; criteria in a "Consequences" block outside the AC section). The 11 parser-only are things the substring structurally cannot see: 9 ordered-marker briefs and 2 whose `- [~]` carries no reason. **The audit and the gate share one parser, so there is no third number.**
  - **The pending suggestions were NOT dismissed** (AC #7, a prohibition). Four scopes, stated separately because conflating them is how this brief started: **396** `igris-ai` gap · **411** `igris-ai` all modules · **734** all-projects gap · **1272** all-projects all modules. Every one identical before and after; nothing dismissed since 2026-05-05. They are the standing reminder until the retroactive ticks are done — dismissal is the last step of TD-075's audit, not the first step of this fix.
  - **TD-075 rescoped, not closed** (operator decision D1). It was driven off the suggestion queue, which was measured as a lossy index: 45% coverage, 2.5× duplication, **194 affected briefs never indexed at all**. A sweep driven off it would have cleared the alarm while more than half the population stayed invisible — and read as "handled". It is now driven off the parser, its bulk-`sed` method is explicitly forbidden as the TD-311 move, and the word "noisy" is gone from the title: three successive agents repeated that framing without checking it, and it is why a correct signal was ignored for twelve weeks.
  - **TWO TESTS PASSED WHILE MEASURING THE WRONG THING, and validation had to surface both.** This is the house standard for this entry, and both were green in a 49-test suite:
    - **`(E5)` re-tested block scoping, not line anchoring.** Its inline `- [ ]` spans sat under `## Problem`, so scoping alone rejected them — the `^[[:space:]]*` anchor could be DELETED with all 49 tests still green. `(E5b)` now puts an inline span on a continuation line *inside* the AC block, the one place scoping cannot help.
    - **`(N3)` pinned one ordering only.** It seeded the dirty brief so it sorted FIRST, so truncating the multi-id loop to `head -1` still refused and the test stayed green — a commit closing a six-criteria-open brief would have landed. `(N3b)` reverses the ordering.
    Root bats 555 -> **557**. The "16-mutant battery, 15 caught" figure predates these and reads as a clean sheet; two demonstrated survivors is the more useful fact.
  - **A third false start, recorded for the same reason:** the parser's first draft forked a subshell per line and took **79.5s** over 447 briefs. Rewritten fork-free to **15.0s**, and the rewrite was proven verdict-identical over all 447 rather than assumed — a faster parser that reads differently is a different parser.
  - **The deferral hatch was already invented, before there was a rule.** `- [~]` + `DEFERRED: <reason> -> TD-XXX` (D2 requires the follow-up id — a deferral with nowhere to go is indistinguishable from one that was forgotten). `[ ]`→`[~]` and `[ ]`→`[x]` are both one keystroke, so there is no asymmetry tempting a false tick. **A tick asserts verified evidence; a deferral asserts the absence of it; a tick made without per-criterion verification invents the evidence, and that is the forbidden move.**

### Fixed

- **The `dense` world is NOT `7d`'s replacement surface — a measured NULL RESULT — and the `KNOWN_RED` ledger can now see an orphaned row (TD-332)** — the brief carried two halves. The first was a relocation; it was evaluated before it was built and it is a **NO-GO**. The second ships regardless and is the durable half.
  - **THE NULL RESULT IS THE DELIVERABLE, and it retires a wrong prescription this repo had already written down twice.** `browser-gate.mjs` and `docs/dashboard.md` both instructed the next reader to relocate `7d` to the `dense` world. That instruction is now replaced by the measurement that refutes it, because without the replacement the file keeps prescribing the rejected move.
  - **The reason it fails: the DENOMINATOR saturates.** `inkSpread` samples the **canvas only**, and every `7d` arm measures at camera **k = 1** — each arm is a fresh graph instance, and `zoomToFit` is reachable only through the FIT button, which `gBr7` never clicks and *cannot*, since the frames it measures precede any possible click. `k_fit` is therefore the visible linear fraction at k=1. Measured on an unfiltered run 2026-08-06 from `G-BR-11`'s own `11-instrument` line: **`k_fit = 0.34271`** at `--graph-column-scale 2` and **`0.13275`** at scale 1 — the canvas shows about a third of the layout's width. The cold arm's *numerator* would stay honest (phyllotaxis RMS `7.07·√N` ≈ 189 units at N=710, well inside the canvas), but **both arms' settled reference saturates against the canvas**. Separation would widen from ~14pp to roughly 50pp and **every point of it would be bought from clipping rather than from layout physics** — the "an instrument that stops measuring reports the reassuring answer" failure this file already refuses twice in writing.
  - **The brief's own gate would have reached NO-GO for the WRONG reason, which is the more instructive half.** It asked whether the `dense` world's `demo` scope differs enough from whole-brain to make the drill non-degenerate. Derived from the fixture: **3 of 710 nodes and 0 of 352 edges** — degenerate, as the brief suspected. But that variable does not drive `7d` at all: the back-out arm seeds from `cachedScope(null).positions` and the cold arm from the `{}` that `putScope` writes on a forced refetch, so both arms are the whole-brain node set with only the position seed differing and neither touches the drilled payload. The drill is a cache-*leaving* manoeuvre; the drilled scope's size is incidental.
  - **A decision rule, derived from the mechanism and fixed before the reading**, so the next brief inherits the criterion rather than re-deriving it: `k_fit ≥ 0.9` → the denominator is honest and the surface is a candidate; `0.6–0.9` → partially clipped, an operator call; `< 0.6` → the denominator IS the canvas, NO-GO. `dense` reads 0.34271, deep in the third band, and the margin survives the box difference between the two gates (a 50% more favourable canvas would still only reach ~0.51). **The residual is an HONEST DENOMINATOR — and it may not be a world at all.** The layout overflows the canvas AT k=1, so either side is fixable: a RIGHT-SIZED world (`k_fit ≈ 1`), or an ANCHORED CAMERA on the world we already have (`G-BR-11` drives absolute zooms with real wheel events and verifies achieved-vs-requested within 5%, so the machinery exists). **TD-366 owns both and must weigh them before building either.** The criterion that decides it is the most transferable thing here: **whichever design is chosen must not put a canvas-dependent quantity back into the reading** — precisely the defect TD-337 removed from `11a`/`11b`, which had been anchored to `k_fit` (a quantity that moves with the canvas box) and were re-anchored onto the absolute `K_FLOOR`. An earlier draft of this entry named only the world, which is the same over-specification this brief exists to retire.**
  - **`7d` stays red and its `KNOWN_RED` row stays with it.** Its recorded reason is rewritten in the same commit, which is the same coupling the ledger enforces in the other direction. The unfiltered run still prints `KNOWN-RED G-BR-7 7d — … owned by TD-332`.
  - **`G-BR-0` — the ledger now audits itself, and this is the half that would have shipped either way.** `KNOWN_RED` keys on a check id **appearing in `results`**, and so do both of its consumers — so a row whose check was renamed or deleted produced **no failure, no `SURPRISE`, and no output at all**. That is the exact dual of the problem the ledger exists to solve: *"an unowned red check is indistinguishable from an accepted one"* arriving from the other side as *"a ledger entry for a check that no longer exists, silently."* `ledger-orphans` asserts every row named a check that ran; `ledger-ids` asserts the id keyspace is still flat, since `KNOWN_RED` keys are gate-**unqualified** (detecting the collision was chosen over re-keying the map — re-keying touches every key and every mutation `gate` field for a defect that does not exist yet).
  - **It is numbered 0 and it runs LAST, and both are load-bearing.** `notRun` is only fully populated once every gate has been reached, and `notRun` is what the gate self-fences on: under `--gates=` it **SKIPS both checks with the filtered gates named**, because a filtered run legitimately never reaches most gates and every row they own would read as an orphan. It is exempt from `--gates=` for the same reason — a guard that silently disappears under a flag is the failure class it exists to close.
  - **Red-first with a planted orphan, made re-runnable.** `--mutate=br0-plant-known-red-orphan` plants a bogus row **on the map itself**, so the run shows what the hole actually looks like: every other consumer reads the planted row and none of them can say a word. `--mutate=br0-duplicate-check-id` re-emits an id that already appeared, with the donor **derived from `results`** rather than hard-coded — a hard-coded donor would emit a unique id and go quietly vacuous the day a check is renamed.

- **Two more of the brief_type vocabulary's unguarded copies (TD-330, TD-331)** — hunted as ONE batch because both are TD-328 follow-ons touching the same vocabulary surface: `brain-mcp-server/src/tools/brief-normalize.ts`, `MAINTAINING.md` and `core/enforcement/brief-type-vocabulary.md`.
  - **TD-330 — the observer had acquired a second, unobserved copy of the vocabulary it observes.** TD-328 fixed `brief_type` drift in the DATA and got the executable path right (the v22 migration and `normalize_brief_types.ts` both IMPORT `CANONICAL_BRIEF_TYPES`), but restated the canonical list BY HAND in `scripts/validate_brief_type_vocabulary.sh` — and that copy is *functional*, it decides the validator's verdict. The only coupling was a pin test that greped a THIRD hardcoded list of 12 names into the bash validator and then checked **three** of them against the TS file.
  - **Measured before fixing, three ways, all of which passed when they should not have:** add a 13th canonical type to the TS array → nothing fails, and the validator reports it NON-CANONICAL forever (a permanent false positive in a gate whose whole job is to be believed); delete `Feature` from the TS array → the pin test still passes; add a bogus entry to the bash array → nothing fails.
  - **`test/validate_brief_type_parity.test.bash` closes all three**, and each was DEMONSTRATED red-then-green rather than asserted. It extracts both definitions and asserts element-identity **in order**. The `CANONICAL_PHASES`/TD-257 pattern applied to a sibling field — the repo already contained the right answer to this exact question. Chose the pin over a generator deliberately: no build step, and a generator would sit between an operator editing a list and the validator honouring it, which is a worse failure mode than a red test.
  - **The guard asserts that it is ARMED.** If either extractor silently returned an empty list, the equality check would compare `""` to `""` and pass while measuring nothing — so a floor check pins that both found the full vocabulary. A floor, not an exact count, or it becomes a second hand-maintained copy of the list, which is the defect being fixed.
  - **The prefix map is guarded too, and TD-330's own exemption had to be narrowed to say so.** TD-330 scope item 3 exempted "the prose copies" and named `/register`'s table among them — then TD-331 changed that table, and the exemption became false in the same release. Eight of the guard's twelve checks now read it, across **both axes** (kind→prefix and prefix→type) and **both representations** (§2's table and §Arguments' bullets). Still exempt: the enforcement doc's derivation table, the validator's header map, and the tool-schema descriptions — each verified unguarded by planting a corruption and getting a green suite. **TD-357** carries the six-copy census.
  - **The exemption also rested on a category error worth naming:** it said those copies restate *the canonical list*. `/register` §2 restates the **prefix map** — a different mapping with different members. That conflation is why the mint surface went unguarded through TD-328 and TD-330 and only got a pin here. What survives: free-form code comments drift visibly enough that pinning them costs more than it saves. What does not: applying that to structured, runtime-read prose.
  - **A LIVE BUG SHIPPED AND WAS CAUGHT IN VALIDATION — the most consequential fact in this batch.** TD-331 fixed `/register` §2's table but left the §Arguments bullet list saying `` `bug` or `feature` → BR-XXX ``. §Arguments comes FIRST in the file, so an agent running `/register feature "..."` reads the stale copy and mints `BR-`, **re-creating the exact collision TD-331 exists to close**. It was mirrored to `~/.igris/`, so the runtime mint surface carried it. A skill file is executed instruction, not documentation, and statement ORDER decides which copy wins.
  - **The pin's first draft went GREEN on the regression it guards.** It filtered `^BR<TAB>` wholesale as "the documented asymmetry", so restoring `| feature | BR |` emitted `BR<TAB>Feature`, got stripped, and passed. Narrowed to filter only the exact row `BR<TAB>Bug`. **A second draft had a blind AXIS**: it captured the kind column in its regex and never used it, so the compact collision `| bug, feature | BR |` left the extracted mapping byte-identical and the whole suite green — while TD-331's entire subject was kind→prefix. Both were caught by planting, not by review, and both are recorded in the test's own comments. *A demonstration that cannot fail is worse than none; so is one that tests the wrong axis.*
  - **TD-331 — `/register` minted both `bug` and `feature` as `BR-`**, the only prefix naming two kinds, at the oldest and largest prefix in the corpus. That collision is the sole reason 20 briefs are permanently untypeable: 17 NULL-type `BR-` rows could not be inferred while every other prefix inferred losslessly (`FR-` 25, `TD-` 21, `TS-` 2, `PF-` 1), and 3 rows typed literally `BR` cannot be folded. **Operator decision 2026-08-06: `feature` now mints `FR-`.**
  - **The canonical-set rule is TIGHTENED, not patched.** Every canonical type with a mint prefix now has exactly one, and `Refactor` remains the single documented prefix-less exception — TD-328's carve-out is not joined by a second.
  - **`BRIEF_ID_PREFIX_TYPES` still omits `BR`, and the mint fix does NOT license adding it.** That table decodes brief IDs that ALREADY EXIST, and all 17 NULL `BR-` rows predate the decision — adding the key would retro-assign exactly the rows TD-331 forbids touching ("an inference that can be wrong should surface, not silently write"). The ambiguity is a property of *when* a brief was minted, not of the prefix going forward. What the decision buys is that the unresolvable set is **capped at 20 rather than growing**; the existing 20 are dispositioned as permanently ambiguous, with the reason recorded.

### Changed

- **The dashboard bundle is split by route, and the byte budget is now a gate instead of a warning (TD-347)** — `cli/dashboard/src` built to ONE chunk, so every UI addition was charged against **484 B** of remaining slack. FR-248 and FR-249 both add UI and could not both fit. Three of the four route pages are now `React.lazy` behind one `<Suspense>` boundary (`Overview` stays eager — see below): **the initial load fell 559,516 B → 285,390 B, a 274,126 B (49.0%) reduction**, with 277,533 B moved to six deferred chunks. TOTAL JS rose 559,516 → 562,923 B (+3,407 B) — the chunking overhead, and the honest cost.
  - **THERE WAS NO CHUNK GATE TO KEEP FAILING, WHICH IS WHY THE BRIEF GREW.** The acceptance criterion said "the chunk gate still fails for an oversized initial chunk — DEMONSTRATED red". But `chunkSizeWarningLimit` is a Vite build-time *warning*: `build-dashboard.sh` exits 0 regardless, and nothing in the test tree asserted a chunk size. **A warning cannot go red.** The gate had to be built — `cli/src/__tests__/dashboard-chunks.test.ts` — and it landed BEFORE the split so its first recorded run was against the unsplit tree at 559,516 B.
  - **THE INITIAL SET IS A LOAD, NOT A FILE**, and that definition is the whole design. It is the entry `<script type="module">` plus every `<link rel="modulepreload">` in `index.html` — what the browser downloads before it can paint. Measuring the entry *file* is gameable: a vendor `manualChunks` split takes the entry file **285,390 → 95,394 B (−189,996 B)** while changing the initial LOAD by **−343 B**. Recorded as plant C; the gate stayed green, correctly.
  - **TWO CEILINGS, because one is a way to spend bytes invisibly.** Plant B put 40 KB behind the lazy Graph route: **INITIAL green and unchanged at 285,390 B, TOTAL red by 16,017 B.** Without the total half, `React.lazy` would be an unbounded budget hole behind a boundary the initial ceiling cannot see — the "it moved elsewhere" defect class. Plant A (40 KB eager) reddens both by 16,023 B. Both ceilings are `measured + 24,000 B` — four briefs at FR-247's 5,899 B, the largest single-brief spend **in a ledger that only starts at FR-246** — and **neither is ever raised to make room** (TD-329's discipline). The superlative is scoped on purpose: FR-240 spent **~+47,700 B**, about 8x that, and one FR-240-shaped brief would bust either ceiling. Kept narrow deliberately — the error runs in the safe direction, and a headroom sized for the worst case on record would absorb that brief silently.
  - **A DEMONSTRATION THAT PLANTS NOTHING IS WORSE THAN NO DEMONSTRATION.** The first plant referenced its bulk as `window.__bulk = BULK.length`; that constant-folds to a number, `BULK` goes unused, and the 40 KB literal is tree-shaken. The build came back **+18 B and green** — which reads exactly like "the ceiling does not catch this". Caught by grepping the built chunks for the literal and finding it absent. A plant must reference the whole value. (An earlier draft used `"x".repeat(40000)` — 20 characters of source, +43 B.)
  - **`15a` PASSED WHILE MEASURING THE WRONG THING, and three sibling checks caught it.** The new browser gate's "cold" loads used `Page.navigate` to a URL differing only in the fragment — a *same-document* navigation, where the document, its module registry and its `performance` timeline all survive. So the cold-load check silently tested the in-session path. It surfaced because `15d` saw a cold `#/overview` fetch all six deferred chunks, `15e` reported min == median == max on all four routes, and `15e`'s armed-check reported `transferSize === 0` every pass. **The check that passed was the broken one.** Fixed by routing through `about:blank` to force a real teardown.
  - **The harness stopped racing a sleep.** `Tab.hash()` / `goto()` waited for `#main` then slept 400 ms; post-split `#main` exists while a Suspense fallback is mounted, so all fourteen existing gates would have raced a chunk fetch. Replaced with deterministic readiness markers — `#main[data-route]` and `[data-route-loading]` — consumed by `Tab.routeReady()`. Product-visible on purpose, and mapped in `MAINTAINING.md`: renaming either silently returns all fifteen gates to sleep-based synchronisation.
  - **G-BR-15 runs FIRST**, because every later gate visits `#/graph` and warms that chunk's immutable cache — first position is the only genuinely cold origin. It asserts data-bearing selectors on 8 route addresses from both cold and in-session paths, zero dynamic-import failures, an enumerated deferral claim, and 20 cache-disabled cold readings that are **recorded, not thresholded**.
  - `Overview` stays **eager**, argued rather than defaulted: it is `router.tsx#parse`'s fallback for `#/` and every unknown hash, and its exclusive weight is small — every import it has is shared with the shell or another route EXCEPT `components/ui/Card.tsx` (~1 KB), so lazying it would buy very little and cost a round trip on the commonest first paint. `gsap` stays eager regardless — `chrome/Cursor.tsx` anchors it, and it is the next planner's candidate. `chunkSizeWarningLimit` survives, lowered 560 → 300 and demoted to a surprise detector; at 560, with the largest chunk now 285.39 kB, it had 274.61 kB of slack and would effectively never have fired again.

### Fixed

- **Two knowingly-red browser gates: one re-expressed and now green, one still red on its own pre-registered rule (TD-337, TD-332, TD-320)** — three briefs hunted as ONE batch because all three live in `cli/scripts/browser-gate.mjs`. Both red gates were pixel proxies shipped failing by explicit operator decision, so the standing warning applied throughout: **a green `7d` or `11a` without a deliberate, argued recalibration is the SURPRISING result, not the reassuring one.**
  - **TD-337 — `11a`/`11b` RE-EXPRESSED, now green, and the boundary is the whole finding.** `nodeWorldSize = px/max(k, K_FLOOR)`, so node *screen* size is `px·min(1, k/K_FLOOR)`. Above the floor node size is pinned while gaps shrink, so zooming out FUSES — that is the pre-FR-244 bug. Below it both scale together and the picture freezes. **At exactly `K_FLOOR` the two size laws are algebraically identical, so the original defect PASSES a reading taken there** — a gate anchored at `K_FLOOR` is a gate the bug walks straight through. The sample moved to `K_FLOOR·0.5` with the denominator AT `K_FLOOR`, both endpoints absolute. The old denominator (blobs at FIT) had moved 358→710 when FR-250 doubled the canvas and was a second, easily-missed coupling; it is gone.
  - **Proven against a real broken build, not a proxy.** Validation rebuilt the product WITHOUT the FR-244 fix: at `K_FLOOR·0.5` it reads **0.3%** against the 95% floor with one blob holding 100% of the ink, while `11b` at `K_FLOOR` stayed GREEN with a component count character-for-character identical to the healthy build. The boundary argument is measured, not reasoned.
  - **`FROZEN_PRESERVATION_FLOOR = 0.95` is derived, not chosen.** Healthy 98.6–99.7% (spread 1.1pp) against defects at 86.9/92.3/81.1%. The populations meet at 92.3/98.6, midpoint 95.45 — the floor sits at the round number *below* the midpoint, deliberately giving 3.3× the healthy spread to the healthy side, because the healthy spread is the one that flakes. 0.98 would be pre-failing at 0.6pp against a 1.1pp spread; 0.90 would pass the defect. `SEPARABLE_BLOB_RATIO = 0.6` was RETIRED rather than moved — it measured a different ratio against a different denominator.
  - **`11e` turns TD-337's structural claim into a standing check, and it costs nothing.** Setting `--graph-column-scale` on the page needs no source edit and no rebuild, so the gate now proves its own readings are invariant to the canvas box. Its mutation reproduces TD-337 on demand: **50.4% at scale 2 beside 98.6% at scale 1, from one run** — a 48.18pp disagreement against a 2pp tolerance. (The 50.4% is TD-337's figure exactly; the scale-1 reading is 98.6%, one component off FR-244's recorded 98.3%, so the two are near-historical rather than identical.)
  - **TD-332 — `7d` STAYS RED, and no threshold moved.** The decision rule was written before the measurement. The absolute arm would re-derive cleanly (15.8pp gap against a 2.1pp spread), but the separation arm's gap is 13.8pp against a **4.1pp** spread, putting any floor ~1.66 spreads from the healthy line — a marginal green, which the pre-registered rule forbids. So the brief was re-scoped to replacing `7d`'s SURFACE rather than tuning a number to make a red test green. **(The `dense` world was then evaluated as that surface and REJECTED — see the TD-332 null result above; the residual is TD-366.)** **What it did close independently: the gate now measures at the viewport it documents** (`setViewport(1440,900)`, cleared in a `finally` so it cannot leak into sibling gates), which alone moved worst-case separation 9.6pp → 13.8pp.
  - **The `KNOWN_RED` ledger turns the prose warning into machinery.** A machine-readable check→brief map; the verdict prints `FAIL — 1 known-red (TD-332)` naming the owner instead of an anonymous red line the next reader learns to scroll past; a **SURPRISE line, and on an unmutated run a non-zero exit, if a known-red check goes green** (armed: forcing it green exits 1; deliberately qualified under `--mutate`, where an injected defect can legitimately move a check and the run exits 0), which is the forcing function that makes the row and its disposition note get removed together; and an `UNINFORMATIVE VERDICT` line when a mutation's predicted victim is already red. TD-337's own rows were removed in the same pass that made them green.
  - **TD-320 — six residual gate gaps closed, the seventh absorbed.** The one that matters: `3-hermetic` asserted 1 of 6 worlds, and now asserts every one plus a harness fail-fast — which is load-bearing because `--gates=7,11`, the way every calibration here was iterated, never reaches that gate at all.
  - **THREE mutations did not bite on first design, and each failure is recorded in the mutation itself rather than quietly fixed** — a mutation that does not bite is a finding. Bundle cost: **0 bytes**, confirmed by Vite emitting the identical content-hash filename.

- **One project with briefs aborted the whole `doctor --remove-orphans` sweep (BR-084)** — `brief_status.project` and `sessions.project` both carry a live FK to `projects(slug)`, and better-sqlite3's bundled SQLite is compiled with `SQLITE_DEFAULT_FOREIGN_KEYS=1`, so the bare `DELETE FROM projects` is REFUSED for any project that still has that history. **Refusing is the safe direction** — an orphaned `brief_status` row would be worse. The defect was what happened next: the throw was unguarded at all four call sites, so it escaped `confirmAndRemoveOrphans` and took the ENTIRE sweep with it. A verb whose whole purpose is bulk cleanup failed wholesale on one reachable input, and every other orphan it could have removed stayed in the registry.
  - **The decision was skip-and-report, and the alternatives were rejected on their blast radius.** Cascading the delete would destroy brief history — the brain's build record, 654 briefs on the operator's own brain — to tidy a registry row, and a `--yes` sweep would take it without asking; re-pointing the briefs is a data migration with no obvious target slug. So the blocked project keeps its row and is reported, and the operator's next move is still available once the briefs are dealt with.
  - **Per-project results, in FR-241 D6's shape.** `deleteProjectRow` no longer throws: it returns `{slug, ok, error}`, and the sweep reports `removed:` / `skipped:` per project and continues. The skip reason names the count and the table that blocked it (`still referenced by 1 brief_status row(s)`) rather than echoing `FOREIGN KEY constraint failed`, which does not tell an operator what to deal with. The blocking tables are DERIVED from the live schema, so a project with 0 briefs and 1 session is named correctly instead of being reported as "0 briefs".
  - **A skipped row still counts as drift**, so the verb keeps that row non-clean and exits 1 rather than reporting a clean sweep it did not perform. **The exit code is not what changed** — measured in both build states, the blocked input exits 1 either way, because pre-fix the escaping throw reached `main().catch` which already set it. What changed is that the sweep now *completes*: the evidence is the surviving rows, not the status code. Recording that because "it now exits 1" reads like the fix and is not.
  - **`rl.close()` moved into a `finally`.** The interactive path leaked its readline interface — and the process's hold on stdin — on every throwing path.
  - **The regression test carries TWO orphans and sweeps the briefed one FIRST.** With one orphan, "aborted after the throw" and "completed with a skip" are indistinguishable: the same single row survives either way. The second, clean orphan is the discriminator — it survives an abort and is removed by a completed sweep. Six tests were driven RED against the pre-fix build first, including the readline leak (`closed === 0` with the interface provably created).
- **Searching the pending-review queue returned approved rows under a banner saying otherwise (BR-085)** — the Learnings tab lets you filter to `pending_review` and search at the same time. Combined, **the filter was silently discarded**: `routes.ts` parsed `review_status` and then forwarded only `{query, project, limit}`, `HybridSearchOptions` had no such field to forward it to, and `hybridSearchLearnings` hard-gated `approved` on both arms — while the UI rendered "SHOWING PENDING REVIEW ROWS" over the results. No error, no empty state, no degraded badge; the rows looked plausible because they *were* learnings, just the wrong set. The pending queue is a review surface, so the reader most affected is the one deciding what to approve.
  - **The decision was argued from measurement, not assumed.** "Searching pending is meaningless" had no factual footing: the `learnings_ai` FTS trigger is unconditional and pending rows are embedded on insert, so both arms are genuinely indexed for them (measured: approved 725/721 embedded, pending 33/33). The `approved` gate became a **parameter defaulting to `approved`**, bound at all three sites — BM25, vector, and the hydration SELECT.
  - **All three, because one is a ranking bug, not a set bug.** RRF scores by each arm's POSITION, so gating one arm silently reorders every surviving row rather than just removing some. Proving that needed a **set-preserving** mutation — un-gate both arms, then filter the fused result back to the same rows: membership passes and the per-row rank assertion fails. A single-arm revert can't show it, because the membership assertion fires first.
  - **The payload now echoes the scope ACTUALLY APPLIED, which is what makes the banner honest during a build skew.** The endpoint resolves the *vendored* brain reader first, so between a source change and a rebuild the new option honours nothing. Rather than let the banner claim a scope the rows lack, the response reports what was applied and names the disagreement. Verified in both build states — with the stale bundle the payload says `approved`, returns approved rows, shows no pending banner, and prints a `REQUEST ADJUSTED` note.
  - **The conscious channel is byte-identical**, measured by running the old and new readers side by side on the MCP wrapper's exact options object: same length, byte-identical rows and retrieval block, keys additive only. `igris_memory_hybrid_search` declares no `review_status` under `additionalProperties: false`, so the model-facing door is unchanged — the widening is operator-facing only, and bounded to a closed two-value vocabulary that cannot reach `rejected` rows.
  - **The class was audited, not assumed to be an instance.** All six `parseFilters` consumers were driven with a recording reader. `learningsSearch` was *also* dropping `category`/`scope`/`provenance` silently — now named by a loop derived from the filter spec at runtime rather than a hand-list. One residual remains and is filed as **BR-086**: `briefsSearch` parses `offset` and drops it unnamed, so paging a brief search returns page 1 again.

### Added

- **The graph canvas is twice as tall, and the page scrolls again on purpose (FR-250)** — an operator note while reviewing the shipped dashboard. `--graph-column-scale: 2` multiplies the derived column, so the canvas goes from ~510px to ~1258px on a 1440x900 display. **This RETIRES a property FR-244 shipped, measured and gated**: that brief removed both the `clamp(420px, 62vh, 900px)` cap AND the page scrollbar (`scrollHeight 1164 -> == innerHeight`). The cap stays retired; the scrollbar comes back, because a canvas that grows on a screen that has not is arithmetic, not a defect. The operator was shown that cost against two alternatives — reclaiming the ~390px of chrome above the canvas for ~1.5x with no scroll, and a fullscreen toggle — and chose this. Still no literal heights: the column derives from `--nav-h`, `--shell-pad-y`, `--shell-pad-b` and `dvh`, and the multiplier is a named token so the gate's bound and the layout's height come from ONE number.
  - **`11c` was re-baselined, not widened.** It asserted "the page does not scroll"; it now asserts the document/viewport ratio sits within a window around `--graph-column-scale`, read out of the page so the check and the layout cannot disagree. A page that stopped scrolling at scale 2 fails too — that would mean the height silently stopped applying — so the check kept its teeth in both directions.
  - **A silent instrument failure was found and fixed, and it had been passing.** `wheelZoomTo` dispatched at the canvas host's GEOMETRIC centre. Once the host is taller than the viewport (1258px in a 900px window) that point is at or past the fold, the wheel never reached the canvas, and the zoom sweep read `k` UNCHANGED after four ticks — reporting the FIT picture at every level below. **`11a` would have read 100% separable, a PASS, for a canvas that was never zoomed.** The interaction point is now clamped to the host's visible intersection with the viewport, which was always the correct place to dispatch.
  - **`11a` and `11b` shipped RED, knowingly, and TD-337 owned them. *(TD-337 has since RESOLVED both by re-anchoring the instrument to `K_FLOOR`; see its entry in this same unreleased set. As recorded at the time:)***  Doubling the column moved `k_fit` 0.13275 -> 0.34271 — 2.6x, on a byte-identical bundle and an identical payload — because `k_fit` is a function of the canvas box. Both calibrations were derived at the old value and now measure a different regime: `11a` samples at `k_fit/2`, which was 0.066 (BELOW `K_FLOOR = 0.11`, nodes clamped at their floor size) and is now 0.171 (above it, nodes scaling normally). Neither threshold was touched. **The picture IMPROVED while the gate went red** — at FIT the taller canvas resolves 710 of 710 nodes against the old 358, largest blob 0.1% of the ink. What is red is the instrument's calibration, not the graph.
  - **Zero JS-chunk cost.** The change is CSS, so it landed in the stylesheet asset (38,679 B) and the app chunk is byte-identical at 559,384 B — the 616 B of slack FR-247 left is untouched.
- **The dashboard can set a brief's priority and attach it to a goal (FR-247)** — two new mutations on the briefs LIST, reached through the existing `POST /api/triage` door. **No new endpoint: the surface is still sixteen GET and one POST**, and that is a measurement rather than a claim — `dashboard.bats`'s exact-set string and `SMOKE_PROBE_PATHS` are byte-identical to their pre-FR-247 values. The frozen delegation map grows five rows → seven (`set_priority` → `igris_brief_update`, `attach_goal` → `igris_edge_create`), which stays the one place a dashboard mutation can be added.
  - **The row SHAPE widened once, because a brief is not addressable by an integer.** The five FR-241 rows key on `ids: number[]`; `igris_brief_update` declares `required: ['project','brief_id']`, and although `brief_status.id` exists and is on the wire, **no brain tool accepts it** — translating id → pair in the server layer would mean a SQL lookup, which that tier's zero-SQL scan forbids by construction. So the body gained `refs: [{project, brief_id}]` beside `ids`, a `target` on each map row says which one it takes, and the two are mutually exclusive with the wrong one refused by name.
  - **A priority-only write READS before it writes, and the reason is a fork in the shipped handler.** `igris_brief_update` is a genuine partial update — but for a brief that exists in `brief_files` with **no `brief_status` row** it takes a row-CREATING branch and writes `title = ''` and `status = 'Ready'`. A priority click would therefore blank a title and invent a status: two writes into the canonical build-state invariant (TD-311) from a request that named neither field. The write path now reads every ref through the FR-240 **read door** (`getBrief` on a `query_only = ON` handle, one connection per POST) and refuses such a ref with the reason stated per item. Measured: **1** such brief exists on the operator brain. The endpoint suite drives the defect RED against the real handler first, then GREEN through the endpoint.
  - **The guard's predicate is `status !== null`, not `record !== null`, and the difference is the whole guard.** `getBrief` LEFT-JOINs `brief_files → brief_status`, so for exactly the population this refuses it returns a **non-null** record whose status columns are all null — a `record !== null` test would have passed every ref it was written to refuse, while looking present and commented. It holds because `brief_status.status` is `NOT NULL`, and that constraint is read out of the live schema by a test so a migration relaxing it fails loudly instead of quietly un-arming the guard.
  - **The picker PRESCRIBES the vocabulary; it does not report it.** Four canonical values plus CLEAR (which sends the empty string and lands as SQL NULL), mirrored from `brief-normalize.ts`. This deliberately departs from FR-245's data-derived rule for the board columns and the filter chips — that rule is right for a surface that REPORTS what the brain holds, and wrong for one that decides what an operator may ASSIGN. Enumerating from data would offer `P4-Trivial` and a bare `P2` as assignable values, minting new instances of a drift that already exists. Measured read-only: 5 bare `P2`, 2 bare `P1`, 1 `P4-Trivial`, out of 1,818 rows. **They stay visible**: the row badge renders the value verbatim, the read filter's options are still data-derived so it remains filterable, and the picker shows a non-canonical CURRENT value as a disabled *not offerable* entry so a brief never silently looks unset. **TD-338 owns folding them** — they arrived through SYNC, an LWW column copy with no normaliser, so folding them here without closing that door would just re-run.
  - **Goal ATTACHMENT ships; goal CREATION is deferred to FR-249, and that is a decision rather than a gap.** `dispatchTriage` discards the tool's success payload, so create-then-attach would need one map row to fire two tools and thread the new goal's id between them — the first exception to *one row is one dispatch*, which is the property that makes the map a review artifact. A `create_goal` row firing `igris_goal_create` alone, with the operator attaching in a second click, preserves it.
  - **`attach_goal` mints a project-ambiguous edge, and says so where it happens.** `entity_edges.from_id` is the bare brief id with no project column, `BR-001` names a different brief in 25 projects, and `getGoal`'s serving-briefs join carries no project predicate. Pre-existing (BR-078) and not fixed here — but the map row DROPS the ref's `project` explicitly, at the point the ambiguity is created, rather than in a builder nobody reads. That join is also an INNER join on `brief_status`, which is the second, independent reason the precondition refuses a status-less brief for attachment too: the edge would be invisible.
  - **The write affordances are LIST-only, by parameter rather than by convention.** `briefRow` is ONE mapper shared by the list and the board (FR-245's own guarantee that a card and a list row cannot drift), so an unconditional checkbox would have put a write path onto the status board — `status`'s own view. The list supplies an affordance builder and the board passes none; the compiler refuses `.map(briefRow)`, a source scan slices the board region of the file, and a browser gate injects the leak into `.record-board` to prove the check can fire.
  - **A brief write can reach the network, and the tests are fenced for it.** `igris_brief_update` emits `brief.synced`; the `sync` component subscribes it unconditionally and, when `auto_push` is configured, pushes `brief_status` and `brief_files` to the remote brain. That is **parity** — an MCP update does the same — and it is documented rather than suppressed. It also means an unfenced fixture write could egress, so every mutating suite arms a fence that points `HOME` at its sandbox AND replaces `globalThis.fetch`, reads both back, and is PROVEN by an arm that configures `auto_push: true` against a fictional remote and requires the blocked request to be observed.
  - **The audit trail gets its first genuinely non-empty parity control.** FR-241's `event_log` differ compared `[]` with `[]` for four of five actions. `brief.synced` is in `EVENT_COMPONENT_MAP` and monitoring subscribes it, so a priority write through the dashboard and the same write through MCP now produce one identical, non-empty `event_log` row each — compared byte-for-byte in two separate processes, alongside a `brief_status` diff. `attach_goal` is the declared-EMPTY control: `edge.created` is in neither the map nor the listen list, so its `[]` is a traced finding, with `entity_edges` carrying the "something really happened" half.
- **Brief search that really is search, and four filters that admit they are filters (FR-246)** — the Briefs tab gains **hybrid BM25 + vector recall** (`GET /api/briefs/search`, RRF-fused at `k = 60`), and Goals / Context Docs / Suggestions / Candidates gain an honest substring `q`. **The endpoint count moves sixteen → seventeen, and that is the ONLY path added**: the other four surfaces took a `q` PARAMETER on paths that already existed, so the contract sweep moved once rather than five times.
  - **Briefs had no lexical arm at all, and the vector arm was thinner than it looked.** Before this, the only retrieval over briefs was `briefs_vec` — and `extractBriefProblem` embeds the **title plus the `## Problem` section only** (falling back to the first 500 characters), is called at CREATE and by the backfill tool and **nowhere else**, and the only trigger on it is a DELETE. So a brief's **body was not searchable at all**, and an edited brief carries a **stale vector**. The new `briefs_fts` index is therefore not merely the offline fallback for the vector arm — **it is the only arm that reaches `brief_files.content`**, and the only one that is current after an edit. (Whether to re-embed on update is a separate brief and is deliberately not fixed here.)
  - **Schema v23: `briefs_fts`, CONTENTLESS, and the size is measured rather than guessed.** Built both ways against a `VACUUM INTO` snapshot of the operator brain (1,814 `brief_status` rows; 1,597 `brief_files` rows totalling 6,211,271 bytes of content): a contentful fts5 cost **+11,452,416 B**, a `content=''` + `contentless_delete=1` one **+3,846,144 B**. The reader needs only `rowid` and `rank` — it hydrates every displayed column from `brief_status` — so the second copy of 6.2 MB of prose would have bought nothing. Six triggers keep it current on both source tables. The sharp edge, invisible in the DDL and verified rather than assumed: a contentless fts5 does **not** reject an INSERT for a rowid it already holds, so every trigger is DELETE-then-INSERT and a test drives all four real writer shapes and pins the resulting row count — because a stale FTS index is indistinguishable from a search that legitimately found nothing.
  - **v23 is additive and still takes a proven backup.** It creates new objects, touches no existing row and **never bumps `updated_at`** (an LWW sync column) because it issues no UPDATE at all. It nonetheless writes a `.pre-v23.bak` and **verifies** it (`PRAGMA integrity_check` = `ok` **and** a matching `brief_status` row count), aborting at v22 if that snapshot cannot be verified. A failed v23 is not fatal: the reader reports `bm25_reason: "brain table absent: briefs_fts (schema v23 not applied)"` rather than silently returning a thinner list.
  - **`igris_brief_similar` did NOT become hybrid.** It is `/register`'s duplicate check and it filters on *cosine similarity ≥ threshold*; a BM25 hit has no cosine similarity to threshold against, so making it hybrid would have quietly changed what counts as a duplicate. It keeps its own pure-vector reader and the two share one `briefs_vec` call site. Its prose is pinned byte-for-byte across the extraction by literal goldens transcribed from the pre-change source — not by a snapshot recorded after it, which would only have asserted that the new code equals itself.
  - **The four substring surfaces say `substring` IN THE PAYLOAD.** `search: {mode: "substring", fields: [...]}`, never a hard-coded sentence in the UI — a sentence is the claim that goes stale when someone swaps the implementation, and no gate can catch a stale sentence. A payload field can be asserted, and `G-BR-13b` does: no surface reporting `substring` may render a recall readout, driven RED on purpose by a mutation that injects one. Substring is proportionate where it is used and the numbers are the argument, measured read-only: **6 goals**, a suggestion queue that is drained rather than recalled over, and five context-doc files of prose (that one is `grep`, and it says `body` rather than a column name because there is no table). Wildcards are neutralised — `?q=%` matches a literal per-cent sign, not every row.
  - **The candidates tab filters BY DESIGN, not by shortcut.** *(Premise since retired — see BR-085 in Fixed, below, which shipped in this same unreleased set. As written at the time: `hybridSearchLearnings` hard-gated `review_status='approved'` on both arms (FR-109) and again on hydration (TD-059), so it **structurally could not return a `pending_review` row**, and routing triage through recall would have answered empty for every query. BR-085 made that gate a parameter, so recall now can. The DECISION survives on its other leg: a queue must be shown exhaustively, in a stable order, with an honest total and continuous pages, while ranked recall returns one fused page with no stable offset semantics.)* Relatedly, `/api/learnings/search` used to parse `review_status` and silently drop it while the UI bannered "SHOWING PENDING REVIEW ROWS"; FR-246 made it **say** it dropped it, and BR-085 stopped it dropping.
  - **Cross-layer search is designed, costed and filed as its own brief.** Its spine is RRF over per-layer ranks — not `igris_graph_brain`, which by construction carries labels only, truncated, with no body content. That makes per-layer retrieval a strict PREREQUISITE rather than a substitute, which is why this brief is the half that ships first.
- **A board view for briefs, and the list stays the default (FR-245)** — `#/layers/briefs` now has two arrangements of the same rows: the list, and a **board partitioned by `status`**. The toggle sits beside the heading and persists in `sessionStorage` for the session (a reload keeps it; a new tab opens on the list). **No endpoint was added — the count stays sixteen.** The column SET comes from `/api/summary`'s existing `briefs.by_status` (a complete `GROUP BY status` over the same scope) and each column's cards and count come from the existing `/api/briefs?status=…`, one request per column. Every displayed count is the `total` from that column's own response; the summary supplies the set and never a number.
  - **The board folds nothing, and the duplicate columns are the data being honest.** `brief_status.status` has no CHECK constraint, and this brain holds three spellings of finished (`Done` 1195, `Completed` 24, `Complete` 1), two of in-flight (`In Progress` 26, `InProgress` 4), one status with a commit hash welded into it and two that are whole sentences. Each gets **its own column with its own count**, because merging is arithmetic over values the system does not know are the same — it would hide a data defect behind a tidy column. **TD-333 owns the status vocabulary.** The only concession is ORDER: spellings that normalise equal sort adjacent, so `InProgress` sits beside `In Progress`. A test pins three separate `Done`-ish columns, so a future "helpful" merge fails rather than ships.
  - **Columns are DATA ∪ VOCABULARY, never a hand-list.** The union of the statuses present in scope with the documented lifecycle (`Draft`/`Ready`/`In Progress`/`Blocked`/`Done`/`Archived`), so a project with nothing in flight still gets an `In Progress` column, and `Superseded`/`Deferred`/`Cancelled` — real values absent from the documented set — still get columns.
  - **`Done` is 75% of the corpus, and the answer is a uniform cap.** Every column shows at most 12 cards with `12 OF 493` in its header and an `OPEN IN LIST →` control that switches to the list pre-filtered to that status. Collapsing `Done` would special-case one *value* of an open vocabulary — the same error as a hand-listed column set, one layer down.
  - **Read-only, measured rather than asserted.** There is no drag-to-change-status and there will not be: `status` is the canonical build-state source. Because "this page issues no writes" is trivially true of a page with no write code, the claim is guarded twice with a positive control on each side — a drag-vocabulary scan with a planted affordance it must find, and a browser gate that drags a card with real mouse events while an in-page counter reports zero non-GET requests **and** non-zero GETs in the same reading.
  - All five existing filters work in both views. The board's `status` filter narrows the COLUMN SET rather than being passed into each column's query; the other four pass through into every column. The board reads once per scope and filter set, stamps `AS OF` and offers `REFRESH` — it deliberately does not follow the shell's 5-second beat.
- **`brief_type` is a vocabulary, not free text (TD-328)** — the column had drifted to **50 distinct non-NULL values plus NULL for ~10 concepts**: `Technical Debt` / `Debt` / `TD` / `TechDebt` / `TechnicalDebt` / `tech_debt` / `Tech-Debt` / `Tech Debt` / `debt` all naming one thing, `Feature` / `Feature Request` / `FR` / `FeatureRequest` splitting one class across four dashboard buckets, and ~16 compounds (`Bug Fix / Compliance`, `Feature / UI Enhancement`) cramming a second fact into a single-value field. Two causes, and fixing either alone fixes nothing: the alias map in `brief-normalize.ts` had only **two** entries, and `normalizeBriefType` ran on **write only**, so no existing row was ever backfilled. TD-328 does both. After the fold: **50 → 19 distinct non-NULL values, 68 → 19 NULL rows, 328 of 1,805 rows changed, zero collateral columns touched.**
  - **The deeper defect, named.** `CANONICAL_BRIEF_TYPES` and the `/register` brief-ID prefix map were never the same set. `/register` mints `DU-` (dependency) and `AC-` (architecture) briefs, but neither `Dependency Update` nor `Architecture` was a canonical type — so those briefs had **no legal type to write**, and one got invented. The canonical set is now **defined as the image of the prefix map ∪ `{Documentation}`**, which gives a mechanical decision rule: *a value with a mint prefix is a type; a value without one is a spelling.* The `/register` §2 table now carries a canonical-type column so the two sets are visibly one, and `MAINTAINING.md` pins that they move together.
  - **`Refactor` is canonical without a mint prefix — a measured exception, deliberately recorded.** The plan recommended folding it into `Technical Debt` and set its own flip criterion: "<70% of `Refactor` rows carry a `TD-` prefix ⇒ promote instead". Measurement said **41%** (19 of 46; 25 were `BR-`, 2 `UI-`), and the `BR-` titles read as genuine refactor work minted under `BR-` only because no refactor prefix exists. The operator declined an `RF-` prefix, so the canonical set is knowingly no longer exactly the prefix-map image. That exception is written into the code, the validator and the enforcement doc so nobody "corrects" it back by applying the rule mechanically.
  - **The correction to insert-narrow / read-widen (#228).** The no-hard-reject half survives untouched — rejecting `Bug (pub.dev Score)` would have meant that brief was never created, and inbound sync rows have no rejection path at all. What does **not** survive is the assumption that tolerance is self-correcting. The old code comment promised "writes get cleaner over time"; the data says they produced 50 spellings, because **tolerance without observation has no gradient**. Read-widen is a *tolerance* policy, not a *silence* policy — a widened read needs an observer or the widening is permanent. Two now exist: a **write-boundary echo** in the `igris_brief_create`/`_sync`/`_update` responses (catches minting, in whichever harness is running) and `scripts/validate_brief_type_vocabulary.sh` wired into pre-commit as **WARN-only** (catches accumulation via remote sync or an older client). Neither rejects; both report. **This costs packed bytes, and an earlier draft of this entry wrongly claimed it did not.** The `cli` npm package BUNDLES the compiled brain server at `dist/brain-mcp-server/dist/**`, so the write-boundary echo (`briefs.ts`) and the fold tables (`brief-normalize.ts`) ship; only the repo-side validator under `scripts/` and the bats trio are genuinely free. The measured own share is recorded in the pack ledger in `cli/src/__tests__/tarball.test.ts` — consult that, never this entry, because the ledger is the surface kept current. The largest single line item is a new packed entry, `dist/brain-mcp-server/scripts/normalize_brief_types.ts`; note that `dist/brain-mcp-server/scripts/` already ships eight comparable maintenance scripts (`td286_renormalize_backfill.ts`, `backfill_brief_edges.ts`, …), so it follows that precedent rather than opening a new class — **do not delete it on sight** as stray weight.
  - **Compounds fold only when the fold is provably lossless.** Each compound folds to its head type **only where the qualifier token already survives in that row's own title or content** (13 of 16 rows qualified). The 3 that did not — plus `Bug/Feature`, which has no head type — are left alone and reported, visible rather than quietly damaged. A `brief_subtype` column was rejected as disproportionate (a 6-file cross-subsystem sweep for 16 rows) but the deferral is a **tripwire, not an omission**: the validator files it as a finding if compounds ever exceed 25 rows or 5% of the corpus.
  - **The 68 NULL rows are all accounted for.** 49 are filled by decoding the mint prefix — a *lossless decode* of a field `/register` assigned from the very type question being asked, filling an absence rather than overwriting a stated value. The other 19 are **explained, not assigned**: 17 are `BR-`, and `/register` maps **both** `bug` and `feature` to that prefix, so inference would silently mistype an unknown number of features; 2 are `INT-`, not a mint prefix at all. That `bug, feature → BR` collision is the same defect class one level up and is flagged for its own brief.
  - **Migration v22, with a backup that is proven rather than assumed.** The fold ships as a WHERE-guarded, bound-param, idempotent data migration touching `brief_status.brief_type` and nothing else — verified against the backup: 0 rows with a changed `updated_at`, title, status, phase, priority, claimed_by or effort, and 0 changed `brief_files` rows. `updated_at` is deliberately **not** bumped because `brief_type` is an LWW sync column and a bumped timestamp would make folded local rows fight an un-migrated remote brain. Unlike v19, whose operation was non-destructive, **v22 aborts at v21 if its `VACUUM INTO` snapshot fails or cannot be verified** (`PRAGMA integrity_check` = `ok` **and** a matching `brief_status` row count) — a fold is unrecoverable from the row itself, so a backup nobody verified is not a backup. A dry-run-**default** backfill script (`normalize_brief_types.ts`) reports every proposed `brief_id · old → new · reason` before any write.
  - **`coding_guidelines.md` §17.2 now records why the widened IN-list stays.** Post-fold, `'FR'` and `'Feature Request'` match zero local rows — and they are **retained deliberately** as defense-in-depth, because an un-normalized row can arrive from a remote brain at any time and the write boundary never rejects one. Deleting them would re-open the exact TD-289 hole for the first row that syncs in. The §17.2 ↔ `/release` Step 0 ↔ bats trio was verified byte-aligned and **unchanged**.

- **`igris dashboard` — the local server verb + application shell (FR-238)** — the first persistent visual surface. `igris dashboard` starts a loopback-only (`127.0.0.1`) HTTP server and opens a browser at it. It was read-only as shipped by FR-238; FR-241 adds exactly one write endpoint (see the security-posture bullet below). Nothing is regenerated: reload and you see the state on disk right now. The shell ports the fifty.dev design language — four runtime palettes (`blood`/`cyber`/`acid`/`mono`), the 3-tier type stack (Anton / Space Grotesk / JetBrains Mono, **vendored as woff2 — no CDN, no network fetch at runtime**), sharp corners, hairline borders, grain overlay, ring+dot cursor, and a `prefers-reduced-motion` block that zeroes every animation (GSAP timelines are gated, not merely slowed). Four read-only endpoints — `/api/health`, `/api/projects`, `/api/summary`, `/api/graph/stats` — all of which return HTTP 200 with a `degraded` field for a missing, empty, unmigrated or corrupt brain, never a 500. FR-239 (graph), FR-240 (layer views) and FR-241 (cognition triage) mount inside this shell.
  - **First external consumer of the FR-237 pure brain-graph builder.** `cli/src/lib/brain-bridge.ts` dynamic-`import()`s the vendored compiled `buildBrainGraph` and calls it with its own read-only handle, honouring MAINTAINING row 105 without breaking the `cli/` ↔ `brain-mcp-server/` zero-cross-import rule. `/api/graph/stats` strips `nodes` and `edges` at the route layer, so the shell physically cannot render a graph that FR-239 owns.
  - **The server layer holds zero SQL.** Reads go through the FR-237 pure builder or the existing MAINTAINING-pinned CLI accessors (`listProjects`, `briefStatusSummary`, `listInstances`) — asserted mechanically in the test suite.
  - **New lifecycle convention.** The CLI's first long-lived verb: foreground, single-instance via `~/.igris/dashboard.lock` over `process-liveness.ts` (pid + start-time, so a recycled pid cannot masquerade as live), signal-released. A second invocation re-opens the running URL and exits 0 rather than binding a second port.
  - **Security posture.** Loopback bind only, `Host`-header allowlist (defeats DNS rebinding), no CORS headers, `nosniff` + `X-Frame-Options: DENY` + CSP `frame-ancestors 'none'` + `Referrer-Policy: no-referrer` on every response, path-traversal guard, and — as of FR-238 — zero write endpoints. FR-241 adds one (`POST /api/triage`) behind method, `Origin`, `Content-Type` and body-size fences.
  - **Zero new runtime dependencies.** Vite/React/Tailwind/GSAP are `devDependencies`; the server is `node:http`. Measured tarball cost: **+156.8 KB packed**. (The +250 KB soft budget this originally cited was retired — see `tarball.test.ts`, which keeps ONE asserted number because a figure nothing executes cannot be caught when it goes stale.)
  - **BR-082 — the scope chip clears to a system-wide view.** `pages/Overview.tsx` was the one page FR-241 did not migrate onto the shared `useProjectScope`: it kept its own copy of the `default_project` ladder and its chip strip had no clear affordance at all, so a page called OVERVIEW could only ever show one project. It now consumes the shared hook and control, and `/api/summary`'s `project` became OPTIONAL — an omitted one drops the predicate on both reads rather than returning `degraded`. The unscoped label is **`everything`**, not "all projects", and the distinction is real rather than pedantic: `instances.project_slug` is nullable, so an active session belonging to no project is in the unscoped count and in no project's count. `briefs.*` does reconcile, because `brief_status.project` is `NOT NULL` with a live FK.
  - **TD-326 — the project-less queue is reachable.** 377 of the 1,210 pending suggestions carry `project_slug = NULL` — synapse's `edge_inference` output, which belongs to the brain rather than to any project — so the project-scoped Triage default could not reach them and nothing said they existed. `/api/suggestions` gained `project_scope=brain-level` (a separate param with a closed vocabulary, deliberately NOT a magic `project` slug, which every other endpoint would have bound verbatim against zero rows) and a `facets.brain_level` count. The scope strip gained a `(brain-level)` chip. The three states are three different sets: `project=<slug>`, `project_slug IS NULL`, and neither — and `brain-level` is the COMPLEMENT of every project, so a bulk action there cannot reach a row any project owns.
  - **FR-244 — the whole-brain canvas is legible when zoomed out.** Node size was drawn at a constant SCREEN size, so zooming out shrank the world between nodes while the nodes stayed 8 px: they had to converge. Measured before it was fixed, in a real browser at Tier C (710 nodes) — the field held **97.5%** of its connected components at two-thirds of fit and **15.9%** one step further out, collapsing to a single blob below that. A percolation transition, not a fade. The fix is a **clamped divisor** (`shapes.ts#nodeWorldSize`): above `K_FLOOR` nothing changes and the 8 px `--s-1` floor is honoured exactly as before; below it the node is frozen in WORLD units, so the field and the nodes in it scale down together as one photograph and every gap survives. Same measurement after: **98.3%** where it had been 15.9% — **annotated by TD-337**: that reading was taken at `k_fit = 0.13275`, so its sample point `k_fit/2 = 0.066` sat BELOW `K_FLOOR = 0.11`, i.e. inside the protected regime the fix governs. The figure was not wrong, it was under-specified: it measured the fix working, in the regime the fix acts in, at a `k_fit` nobody knew was transient. The equivalent reading anchored to `K_FLOOR` rather than to `k_fit` is 98.6-99.7% across two column scales. `K_FLOOR = 0.11` is derived from the sweep, not picked, and the docstring carries the portable form of the derivation (the ratio `0.66 · k_fit`) because the absolute figure is a function of the canvas box and the payload.
    - **One law, five call sites.** The paint, the pointer-area buffer, the selection ring, the pointer-capture size and the label obstacle boxes all route through one function. A sixth open-coded `sizePx / globalScale` is refused by a source scan with a self-negative-control — a call site keeping its own division would make hit-testing disagree with the rendered picture at low zoom, which presents exactly like FR-239's dead canvas. **Two floors are traded, and both are written down:** the 8 px node floor and the 44 px coarse-pointer capture area, which scales with it below `K_FLOOR` so a capture radius cannot outgrow the shrinking nodes around it and select the wrong one.
    - **The canvas takes the vertical column.** (FR-250 later DOUBLED that column and, with it, gave the page scroll back by operator decision — the retired CLAMP below stays retired.) `.graph-surface`'s `clamp(420px, 62vh, 900px)` is retired — no viewport however tall could show more than 900 px of canvas — and the query twin moved from a full-width block below the layout into the side column beneath the inspector, which is what freed the height. Exemption 04 asks for adjacency, not for a particular edge, and the twin renders in both branches so the no-nodes scope still carries one. At 1440x900 the page no longer scrolls (it did: 1164 px against 900); at 1600 px tall the canvas is 1210 px.
    - **The DENSITY banner is an overlay, and that is a bug fix twice over.** It is the one banner whose visibility is computed from the canvas's own ResizeObserver box, so with a full-column canvas a page-row banner could mount → shrink the canvas → refire the observer → unmount → grow. Out of flow, that loop cannot start. It also carries `pointer-events: none`: an opaque out-of-flow strip over the canvas ate every hover, click and wheel that landed on it — caught in review, and now asserted by a gate check with a mutation that restores the defect on purpose.
    - **`tracePath` is untouched, deliberately.** The brief's first AC asked for circular nodes; it was withdrawn at sign-off. At Tier C the chrome is `silhouette` — no border, no dash, no glyph — and colour encodes interaction state rather than role, so shape is the entire role signal in exactly the view the complaint came from. Legibility was the goal, circles were a proposed means, and the means was dropped rather than the goal.
    - **A new browser gate, and a metric repair validated by a mutation built first.** `G-BR-11` drives real wheel events over a sixth (Tier C) sandbox world and counts connected components of canvas ink; it was written and recorded RED against the unfixed tree before the fix existed. `G-BR-7`'s `7d` then went red, and the cause was **not** the size law — proven by reverting the layout and re-running — but `inkSpread`, which accumulated its moments in grid-cell INDEX units and was therefore silently coupled to the canvas aspect ratio. It now weights by cell pixel size. Because repairing a metric in the same breath as discovering it went red is indistinguishable from tuning it, `7d` was first given its own mutation, proven to bite against the OLD metric, and re-proven after. **`7d` remains red and its thresholds were not touched** — the residual and every figure are recorded in the gate's own notes and in `MAINTAINING.md` row 109.
  - Adds `cli/src/lib/open-url.ts`, a TypeScript port of the cross-platform browser ladder that until now existed only as bash inside `core/skills/visualize/SKILL.md`. The bash original is deliberately left in place — collapsing the two is TD-308's job.
  - Docs: `docs/dashboard.md` (verb, API, lifecycle, security posture) and `cli/dashboard/PORTING.md` (every ported file mapped to its fifty.dev origin, with each deliberate divergence recorded).

### Fixed

- **The `cli` vitest suite ran in no push/PR workflow** — `cd cli && npm test` was gated only by `npm-publish.yml`, which triggers on version tags, so a cli unit test could be red for weeks and only surface at release. Added a `Run CLI vitest suite` step to `test.yml`'s existing `cli-bats` job (which already does `npm ci` + build). Same gap class TD-303 closed for the bats suite. The new `cli/dashboard/` app had the identical problem for a different reason — `vite build` strips types without checking them and the app's tsconfig is isolated from the CLI's — so a `typecheck:dashboard` script and a matching CI step were added beside it.

---

## [7.2.1] - 2026-08-12

> RELEASE AUDIT BYPASSED (IGRIS_BYPASS_RELEASE_AUDIT=1): FR-236, FR-243, FR-251, TD-345

Hotfix off `v7.2.0` — install-path fixes only.

### Fixed

- **`npm install -g igris-ai` fails on Node 26 (BR-089)** — `better-sqlite3`
  bumped to `^12`, which declares the 20.x–26.x range this package already
  advertises in `engines.node`. Migration now runs with
  `PRAGMA trusted_schema = ON` and restores `OFF` afterwards, so the v19
  `ALTER TABLE … RENAME` no longer trips SQLite's virtual-table trigger check.
- **Packaged `igris` bin is not executable (TD-373)** — `npm run build` now
  restores the exec bit on `dist/index.js`.


## [7.2.0] - 2026-07-05

### Added

- **`/setup` — first-run onboarding + reconfiguration (FR-235)** — fresh
  installs get a guided first run: `igris init` ends with a human
  next-step, `/boot` shows a one-time Welcome, and the new `/setup` skill
  teaches the register→hunt→rest loop with a consented, repo-safe first
  `/ground`. Returning users get a re-runnable settings editor (shells the
  existing `igris configure` verb) covering identity, remote brain, and
  cognition toggles, plus three new `USER.md` preferences (addressing,
  notification style, auto-approve threshold). Adds a hidden
  `igris onboarding` verb and a `config.json`
  `onboarding.{completed,boot_welcomed}` first-run flag.

### Changed

- **`igris init` final report** now leads with the human next-step;
  technical details move behind `--verbose`.

### Fixed

- **`igris init --upgrade` tripped the config.json preservation guard on
  the onboarding stamp (BR-077)** — the FR-235 onboarding-completed stamp
  reserialized `config.json` on upgrade, breaking the byte-for-byte
  preservation test. The stamp is an additive, sibling-preserving system
  write — all user-authored config values are provably unchanged.
  Corrected the misleading `init.ts` comment and refined the `init.bats`
  upgrade test to assert `USER.md` byte-for-byte and `config.json`
  user-data unchanged (number-normalized), with the additive onboarding
  key as the only permitted delta.

---

## [7.1.0] - 2026-07-04

### Changed

- **`igris sync code` pre-restart smoke check** — the `better-sqlite3`
  `node -e 'require(...)'` smoke gate now runs BEFORE `pm2 restart`
  instead of after. If the native binding fails to load, the deploy
  aborts and the previously-running brain process is left untouched,
  vs the prior behavior where pm2 restart was issued first and the
  new process crashed on next DB access. Smoke is a standalone node
  subprocess that reads only filesystem state, so success-path
  behavior is unchanged. Closes #TD-141 (supersedes the post-restart
  trade-off taken in TD-135).

### Added

- **Project-handoff line (FR-229 / FR-230 / FR-231 / FR-232)** — a
  complete cross-installation project-portability workflow:
  - **`igris export <project>`** (FR-229) — read-only verb that
    serializes a project's brain slice (briefs + brief-graph + context
    docs + goals by default; `+learnings/errors/concept-graph` at
    `--tier full`) into a portable `.igris-pack.tar.gz` bundle. Supports
    `--out`, `--tier core|standard|full`, `--include`, `--since`.
  - **`igris import <bundle>`** (FR-230) — the consumer half. Unpacks a
    `.igris-pack.tar.gz`, verifies its checksum, and rejects
    executable-surface/unknown stores before any DB write. Classifies
    every row NEW/UNCHANGED/INCOMING/LOCAL_ONLY/CONFLICT via an
    ancestor-based 3-way compare (bundle hash vs. local hash vs. a
    recorded ancestor hash in a CLI-local ledger under
    `~/.igris/projects/<slug>/imports/` — deliberately NOT a naive
    `updated_at` last-writer-wins), previews the plan, requires explicit
    confirmation, and applies the chosen `--on-conflict
    ask|theirs|mine|newer` policy in one transaction. Auto-registers the
    target project on a fresh machine so the `brief_status` foreign key
    is satisfied atomically; `--dry-run` previews with zero writes;
    `--as <slug>` imports under a different slug. A partial apply exits
    non-zero and stays retryable; BLOB content (context-doc bodies)
    round-trips losslessly.
  - **`/handoff` skill** (FR-231) — wraps the export/import verbs with an
    in-chat preview/confirm ceremony, and records the project-slice
    portability contract in the OS knowledge-map (tiers + exclusions,
    and the two-mode portability decision: VPS sync for same-owner
    continuous access vs. bundle export/import for cross-owner
    point-in-time handoff).
  - **README + brand assets** (FR-232) — new project-handoff section
    documenting the `/handoff` export/import workflow, plus a top brand
    banner and a 2400×1260 OG/social preview card generated via the
    fifty.dev `oss-readme` asset pipeline.

- **`igris sync code` advisory for `.claude/*` real dirs** — warns
  (does not abort) when any of `.claude/{agents,rules,skills}/` is a
  real directory rather than a symlink. RSYNC_EXCLUDES treats these
  as symlinks per the v6 install model; a real directory would have
  its contents silently stripped at deploy time. The advisory surfaces
  the partial-install footgun before it bites. Closes #TD-139.

- **Multi-harness distribution redesign** — consolidated per-harness
  wiring (agents, skills, MCP servers, hooks) onto one descriptor model
  with a single `igris harness` compile/check surface and an `igris
  registry` verb for Layer-2 overlays. Cursor onboarded as a 6th
  first-class harness alongside Claude, Codex, Gemini, OpenCode, and
  Antigravity; drift-guard parity extended across agent, skill, MCP,
  and hook surfaces so a stale projection is caught the same way on
  every harness instead of only Claude's. (FR-150 epic, FR-161/164/165,
  FR-171/172/179, FR-202, FR-217, TD-283, and related harness/registry
  work)
  - **Correction (TD-367, 2026-08-10):** this entry called Cursor a "6th
    *first-class* harness". Cursor was and is the 6th declared harness —
    the onboarding shipped, and it is wired end-to-end — but under the
    tier definition added by TD-367 (`docs/multi-cli.md` § Harness tiers)
    it is a **bridge** harness: `hooks.supported: false`, so Igris's gates
    are advisory there. Only the tier word was wrong. The text above is
    left byte-unmodified on purpose: a shipped changelog is a historical
    record, corrected by note rather than rewritten.

- **Bundled brain — cognition engine (review-gated, off by default)** —
  the `igris-brain` MCP server bundled into this package gains a shared
  cognition host running single-purpose LLM instances that observe the
  brain and *propose* candidates for review; nothing is auto-written to
  conscious memory. Instances: `perception` (transcripts → learnings),
  `subconscious` (digest → suggestions), `synapse` (learning-to-learning
  edge inference), and the `janitor` family (`arbiter` contradiction
  resolution, `curator` stale-learning pruning, `cartographer` Leiden-
  clustering meta-learnings). Every instance and every auto-apply flag
  defaults to disabled/`false` in `~/.igris/config.json`; the new `igris
  configure` verb onboards persona/VPS/cognition opt-ins. Documented in
  `docs/COGNITION.md`. (FR-118, FR-116, FR-210, FR-211)

- **Bundled brain — knowledge-graph cluster** — new graph tools (node
  CRUD, search, dashboard, graph render/visualization), store-time
  knowledge-graph edge population, and goals + metrics dashboards with
  a drift validator.

### Fixed

- **Global install left the bundled brain MCP unbootable (BR-075)** —
  `npm install -g igris-ai` set `npm_config_global=true` in the
  postinstall's environment, which the bundled brain's nested install
  inherited and aborted on, leaving `dist/brain-mcp-server/node_modules`
  empty so `igris-brain` crashed on spawn with `ERR_MODULE_NOT_FOUND`.
  Fixed by sanitizing the child environment (stripping inherited
  `npm_config_*` vars) so the nested install always runs as a clean
  local install. Verified in a Docker clean-room (`npm install -g` →
  `igris init` → brain boots). Shipped as an interim `7.0.1` hotfix
  ahead of this release; recorded here for completeness.

- **`igris sync code` Python cache coverage** — `*.pyo` and `*.pyd`
  were missing from `RSYNC_EXCLUDES` despite being covered by
  `.gitignore`'s `*.py[cod]` glob. Added both, plus a new vitest
  audit (`gitignore-sync.test.ts`) that parses `.gitignore` and
  asserts every (non-skip-listed, glob-expanded) pattern is mirrored
  in `RSYNC_EXCLUDES`. Catches future drift programmatically. Closes
  #TD-140.

- **`igris init`/`refresh` dry-run output** — `--from-source --dry-run`
  previously printed `rename: <core> -> <core>` for the core/ deposit,
  but the actual non-dry path uses recursive `copyFromSource(...)`.
  Added a `wouldCopy()` primitive to `DryRunCollector` with a
  dedicated `copy:` printer block, and switched both verbs to use it.
  Closes #TD-142.

- **`igris init` interactive prompts** — `igris init` now interactively
  prompts for user identity (name, email) and optional remote_brain
  config (URL + API key). Previously, USER.md shipped with literal
  `{{USER_NAME}}` / `{{USER_EMAIL}}` placeholders that the user had to
  hand-edit after install, and `--skip-remote` was effectively a no-op
  (it gated a prompt that did not exist). `--yes`, `--upgrade`,
  `--dry-run`, and non-TTY shells all auto-skip prompts using defaults
  so CI and `curl | bash` installers never hang. Closes #TD-144.

- **brief-gate hook reads the brain DB** — `pre_tool_use.sh` now queries
  `brief_status` (sqlite3) for an `In Progress` brief instead of grepping
  `~/.igris/projects/<slug>/briefs/` only — v5+ briefs (brain-only, no
  filesystem cache) were invisible to the gate, so a legitimately-active
  brief still produced "No active brief found". It also resolves the
  project slug by walking `PROJECT_DIR`'s ancestors against `projects.path`
  in the registry, so a subagent whose cwd is a subdirectory (e.g.
  `<repo>/cli`) no longer resolves the wrong slug via `basename`. Both new
  paths degrade to the legacy v4 filesystem-cache behavior when sqlite3 or
  the brain DB is unavailable; the resolved slug is validated against
  `^[a-z0-9_-]+$` before any SQL interpolation. New
  `cli/tests/integration/pre-tool-use-hook.bats` (5 cases). Closes #TD-146.

---

## [7.0.1] - 2026-05-11

### Fixed

- **`igris sync code`** — workstation `node_modules/` is no longer rsynced
  to the VPS. Native modules built on the dev workstation (e.g. macOS-
  arm64 `better-sqlite3`) crashed when loaded on the Linux x86_64 VPS.
  The new pipeline runs `npm ci` on the VPS post-rsync so native bindings
  are Linux-native, then runs `npm run build` for `brain-mcp-server/`,
  then `pm2 restart igris-brain`, then a `require("better-sqlite3")`
  smoke check that fails loud if the native binding can't load. Closes
  #TD-135.

### Changed

- `igris sync code` rsync now applies an exclusion list mirroring
  `.gitignore` (`node_modules/`, `.git/`, `dist/`, `build/`, IDE files,
  logs, temp files, `*.tar.gz`, etc.). `--dry-run` enumerates the full
  exclusion list for audit.

---

## [7.0.0] - 2026-05-08

First public npm release. Renamed from `@igris-ai/cli` to `igris-ai`.

### Added

- **`igris init`** — bootstrap a fresh `~/.igris/` (or upgrade an existing
  v6 install) from a GitHub release tarball or a local source repo.
  Supports `--from-source <path>`, `--channel <ref>`, `--upgrade`,
  `--skip-remote`, `--cli-bridge <list|none>`, `--dry-run`, `--yes`.
- **`igris refresh`** — re-fetch `~/.igris/core/` from the recorded
  channel (or switch channels). Supports `--from-source`, `--channel`,
  `--no-propagate`, `--dry-run`, `--yes`. Cache-fast-path: same SHA →
  no-op.
- **`igris install <path>`** — full native TS pipeline for installing
  Igris in a project: `.claude/settings.json` hooks block (merged),
  `.claude/{agents,rules,skills}` symlinks, `CLAUDE.md` regen,
  `.igris_version` marker, brain registry row, `installed_features.json`.
  Supports `--slug`, `--no-hooks`, `--dry-run`. Hooks installed by
  default (TD-100 silent-failure inversion).
- **`igris update`** — update materialized layer for one or more
  projects. Supports `--all`, `--slug <slug>`, `--self` (npm self-
  upgrade), `--dry-run`.
- **`igris register-project [path]`** — write the brain registry row
  only (no `.claude/`, no hooks, no `CLAUDE.md`). Supports `--slug`,
  `--allow-missing-path`.
- **`igris sync <code|data|all|status>`** — push code/data to the VPS
  brain. Replaces `scripts/igris_vps_update.sh`. Supports `--dry-run`,
  `--if-changed` (cron parity).
- **`igris doctor [--fix] [--remove-orphans]`** — read-only diagnostic
  walk over the registry. Reports drift across 12 classes:
  - `path-missing` — registry row points at a deleted dir
  - `not-installed` — path exists but `.claude/` missing
  - `hooks-missing` — settings.json present but no Igris SessionEnd hook
    (the TD-100 silent-failure class)
  - `hooks-stale` — Igris hooks present but command path differs from
    canonical
  - `slug-basename-mismatch` — informational; row.slug != basename(path)
  - `duplicate-path` — multiple slugs share a single realpath
  - `symlink-target` — registered path is itself a symlink
  - `brain-core-missing` (NEW in 7.0) — `~/.igris/core/` absent or empty
  - `brain-core-stale` (NEW in 7.0) — `~/.igris/core/` content hash
    diverges from configured channel head
  - `channel-mismatch` (NEW in 7.0) — per-project `cli_version` ahead
    of current CLI
  - `bridge-missing` (NEW in 7.0) — CLI on PATH lacks configured bridge
  - `clean` — none of the above
  `--fix` repairs `not-installed`, `hooks-missing`, `hooks-stale`,
  `brain-core-missing` (invokes `runRefresh()`), and `bridge-missing`
  (invokes partial-mode `runInit()`).
- **`--dry-run` flag** — supported on every state-changing verb (init,
  refresh, install, update, sync). Prints a non-destructive plan.
- **GitHub Actions npm-publish workflow** — `.github/workflows/npm-publish.yml`
  triggers on git tags matching `v*.*.*`. Gated behind
  `secrets.NPM_TOKEN` — workflow lands behind `workflow_dispatch` until
  the npm org is registered (Risk #3).

### Changed

- **Distribution model** — moved from `npm link` (Phase 1, MG-013) to
  `npm install -g igris-ai`. The package is renamed from `@igris-ai/cli`
  to `igris-ai` per the V7 distribution decision (D-2 lock).
- **Brain content delivery** — `~/.igris/core/` is now sourced from a
  GitHub release tarball at install time (`igris init`). The repo's
  `core/` directory remains canonical for development; end users no
  longer need a `git clone`.
- **Hooks default** — hooks ARE installed by default. Pass `--no-hooks`
  to opt out. This is the v7 inversion of v6 behavior, fixing the
  TD-100 silent-failure root cause.
- **CLI naming** — package binary stays `igris`; only the npm package
  name changes. `igris --version` reports `7.0.0`.

### Removed

- **`scripts/igris_brain_init.sh`** — replaced by `igris init`.
- **`scripts/igris_brain_refresh.sh`** — replaced by `igris refresh`.
- **`scripts/igris_install.sh`** — replaced by `igris install` (M2).
- **`scripts/igris_migrate_to_v4.sh`** — v3→v4 migration retired. The
  script invoked `scripts/igris_brain_init.sh` (deleted in M5) and
  `scripts/igris_brain_refresh.sh` (deleted in M5); it had been
  functionally broken at runtime since the M5 native-CLI cutover.
  Users on v3 should reinstall via `igris init` + `igris install`.
- **`scripts/igris_update.sh`** — replaced by `igris update` (M3).
- **`scripts/igris_vps_update.sh`** — replaced by `igris sync code` (M4).
- **`scripts/igris_cli_sync.sh`** — absorbed into `igris install` (M2).
- **`scripts/igris_hooks_sync.sh`** — absorbed into `igris install` (M2).
- **`scripts/validate_canonical_hooks.sh`** — the validated source
  (`scripts/hook-adapters/install_claude_hooks.sh`) is itself deleted in
  this release; the validator becomes a no-op.
- **`scripts/hook-adapters/install_claude_hooks.sh`,
  `install_codex_hooks.sh`, `install_opencode_hooks.sh`,
  `_common.sh`** — the native TS at
  `cli/src/lib/{canonical-hooks,bridges}.ts` is the sole hook writer.
- **Legacy bats fixtures** — `test/igris_init.test.bash`,
  `test/igris_hooks_sync.test.bash`, `test/igris_cli_sync.test.bash`,
  `test/validate_canonical_hooks.test.bash` are removed; their coverage
  is replaced by `cli/src/__tests__/*.test.ts` and
  `cli/tests/integration/*.bats`.

### Migration

For users on v6:

```bash
# 1. Diagnose current state
igris doctor

# 2. Preview the upgrade (no writes)
igris init --upgrade --dry-run

# 3. Apply the upgrade
igris init --upgrade

# 4. Preview project propagation
igris update --all --dry-run

# 5. Propagate to all registered projects
igris update --all
```

The `--upgrade` flow preserves `~/.igris/memory/knowledge.db`,
`~/.igris/USER.md`, and `~/.igris/config.json` byte-for-byte.

### Internal

- 19 new vitest cases across 4 files (drift detectors).
- 1 new bats integration file (`doctor-drift-classes.bats` — 8 cases,
  one per drift class).
- Total CLI test surface at 7.0.0 close: ~246 vitest + ~62 bats.
- `npm pack --dry-run` ships only `dist/`, `README.md`, `CHANGELOG.md`,
  `package.json`. Source TypeScript and tests are not published.

---

## [Pre-7.0]

Earlier development phases of the CLI shipped as `@igris-ai/cli` via
`npm link` only (no public registry). See the repo-level
`/CHANGELOG.md` for the broader Igris AI release history.
