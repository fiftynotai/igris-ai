/**
 * Brain Engine v5.0 — Storage Adapter Interface
 *
 * Re-exports the StorageAdapter interface from types.ts.
 * This file exists as the canonical import point for the storage layer.
 *
 * @module engine/storage/adapter
 * @author Fifty.ai
 */

export type { StorageAdapter, Migration } from '../types.js';
