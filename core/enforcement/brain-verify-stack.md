---
obligation: "Verify-stack — confirm a project's tech-stack/archetype before a cross-project recommendation"
mechanism: honor-system
status: honor-system
lives_in: "core/os/memory.md (decision triggers)"
summary: "Brain obligation #5. No gate enforces it; the model verifies the target project's stack/archetype (e.g. via igris_project_status) before recommending across projects."
---

# Brain obligation #5 — Verify-stack

Before a cross-project recommendation, verify the target project's tech-stack and
archetype rather than assuming. Honor-system reach — the cautionary tale is the
dark-theme incident (recommending a pattern that did not fit the project's stack).
