<!-- GENERATED by core/scripts/gen_enforcement_registry.sh — DO NOT EDIT BY HAND. -->
<!-- Re-run the generator to refresh; edits here are overwritten. -->

# Enforcement Registry — Obligations → Mechanism

The model-facing registry of "what's forced and how". The **discovery** generator
builds this by scanning each obligation's self-describing frontmatter
(`core/enforcement/*.md`). It is the authoritative obligation→mechanism map the
conduct layer points at.

- **mechanism** = how the obligation is enforced: `gate` (a hook/validator/skill
  step that blocks or surfaces), `automation` (a pipeline does it), or
  `honor-system` (the model is trusted to follow it — no gate).
- **status** = whether the mechanism is live (`shipped`), trusted
  (`honor-system`), or pending another brief.
- **lives_in** = where the mechanism is implemented (the file/skill that enforces
  it), or `—` for a not-yet-built mechanism.

> Consumers: `core/os/conduct.md` (the obligations point here) and
> `core/os/self-maintenance.md` (the enforcement-layer extension rule).

| obligation | mechanism | status | lives_in | summary |
|---|---|---|---|---|
| Brain enum values in the schema must appear in the actor-facing stewardship doc | gate | shipped | scripts/validate_brain_stewardship_enums.sh | Pre-commit DRIFT-1 validator fails when an enum declared in the memory component schema is missing (verbatim, backticked) from the brain-stewardship section, or diverges across duplicate enum blocks. |
| Brief-first — a brief must exist before any file modification | gate | shipped | core/hooks/shared/pre_tool_use.sh | PreToolUse hook blocks Write/Edit when no Active Brief is in progress; one-shot escape via IGRIS_BYPASS_BRIEF_GATE. |
| Check-dependents — inspect graph neighbors before a refactor | honor-system | honor-system | core/os/memory.md (decision triggers) | Brain obligation #6. No gate enforces it; the model checks the knowledge-graph dependents of what it is about to change before refactoring. |
| Consumer sweep — re-point every consumer when a mapped contract changes | gate | shipped | scripts/check_contract_consumers.sh | FR-186 pre-commit checker parses MAINTAINING.md, WARNs on rename/delete of a mapped token, and hard-fails on a stale-map citation (a consumer path that no longer exists). |
| Dup-check — call brief_similar before creating a brief | gate | shipped-this-brief | core/skills/register/SKILL.md | Brain obligation #3. FR-199 wires igris_brief_similar (threshold 0.85) into /register as a creation-time gate-step: a hit >= threshold stops for operator confirm; tool-unavailable fails open. |
| Error-lookup — look up an error in the brain before debugging it | honor-system | honor-system | core/os/memory.md (decision triggers) | Brain obligation #4. No gate enforces it; the model follows the memory-layer decision triggers and queries prior errors before debugging. |
| Harness parity — committed per-harness artifacts must match the canonical sources | gate | shipped | scripts/validate_harness_drift.sh | Pre-commit harness-drift validator fails when a staged harness artifact (skills/agents/identity/MCP projection) diverges from its canonical source. |
| igris_os.md section line-ranges in igris_tree.json must stay accurate | gate | shipped | scripts/validate_igris_tree_lineranges.py | Pre-commit DRIFT-3 validator fails when an igris_os.md section edit crosses a SECTION marker without bumping the matching context_files.igris_os.sections.<name>.lines range. |
| Mirror sync — a repo core/ file copied to its runtime mirror must be byte-equal | gate | shipped | core/scripts/verify_mirror.sh | TD-096 primitive verifies byte-equality between a repo core/ source and its ~/.igris/core/ runtime mirror; self-evidencing per-pair verdict, non-zero exit on any MISMATCH/MISSING/SAME_INODE. |
| Phase discipline — no commit while the active brief is in BUILDING or TESTING | gate | shipped | scripts/git-hooks/pre-commit | Pre-commit phase guard discovers the active brief and blocks the commit when phase IN (BUILDING, TESTING); one-shot escape via IGRIS_BYPASS_PHASE_GUARD. |
| Recall — query the brain before a decision with likely precedent | honor-system | honor-system | core/os/memory.md (decision triggers) | Brain obligation #1. No gate enforces recall; the model follows the decision triggers in the memory layer before deciding. |
| Skill/agent frontmatter must parse under strict YAML | gate | shipped | scripts/validate_skill_frontmatter_yaml.py | Pre-commit validator runs every SKILL.md frontmatter block through strict PyYAML safe_load — catches the unquoted colon-space scalar that silently skips a skill on Codex. |
| Skills and the OS core must name only abstract intents, never harness-specific branches | gate | shipped | scripts/validate_skill_os_harness_leak.py | Pre-commit validator scans core/skills/*/SKILL.md + core/os/*.md + core/prompts/*.md for known harness-leak token classes (CLAUDE_CODE_* flags, per-harness config-path targets, define_subagent/invoke_subagent) and hard-fails on a genuine instruction-level leak; an explicit allowlist exempts the adapter-authoring/by-design files. |
| Store — capture the lesson after a non-obvious fix or discovery | automation | honor-system (automation pending FR-118) | — | Brain obligation #2. Intended to be automated by the perception/store-after-lesson pipeline (disabled in v7); that re-enable is FR-118. Until then, store is honor-system. |
| Verify-stack — confirm a project's tech-stack/archetype before a cross-project recommendation | honor-system | honor-system | core/os/memory.md (decision triggers) | Brain obligation #5. No gate enforces it; the model verifies the target project's stack/archetype (e.g. via igris_project_status) before recommending across projects. |
