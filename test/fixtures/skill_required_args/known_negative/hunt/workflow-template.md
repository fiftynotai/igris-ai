
### INIT
- Load brief via MCP (`igris_brief_get` with `project` and `brief_id`, both REQUIRED)
  or cache (`~/.igris/projects/{project}/briefs/`)
- Verify status (Ready or In Progress)
- Update session state
